# 多场景中心专项测试方案与执行报告

## 1. 测试目标

验证飞书《多场景探索》中 12 个场景已完整进入产品，用户可从页面选择模板、创建运行实例、推进步骤，并且任何高风险动作都不能绕过审批。

## 2. 覆盖范围

| 测试域 | 内容 | 当前结果 |
|---|---|---|
| 场景完整性 | 12 个场景、Agent、步骤、连接、指标和审批策略 | 通过 |
| API | 列表、详情、筛选、创建运行、查询运行 | 通过 |
| 状态机 | running、waiting_approval、completed | 通过 |
| 审批安全 | 未审批推进 409、审批只对当前高风险步骤生效 | 通过 |
| 前端 | 分类筛选、模板详情、任务启动、进度、审批和审计 | 构建通过，浏览器联调通过 |
| 回归 | 智能问数、SQL 优化、数据集和 AIOps 原接口 | 通过 |

## 3. 核心用例

| ID | 场景 | 预期 |
|---|---|---|
| SCN-001 | 查询场景列表 | 返回 12 个模板 |
| SCN-002 | 按“安全治理”筛选 | 只返回安全漏洞响应流水线 |
| SCN-003 | 创建脚本修复任务 | 创建根实例，首步骤运行中 |
| SCN-004 | 连续推进只读/测试步骤 | 步骤完成并记录证据 |
| SCN-005 | 推进 Reviewer 高风险步骤 | 进入等待审批，不执行动作 |
| SCN-006 | 未审批再次推进 | HTTP 409 |
| SCN-007 | 审批 Reviewer 步骤 | 只批准当前步骤 |
| SCN-008 | 到达发布高风险步骤 | 再次要求独立审批 |
| SCN-009 | 两次审批后完成发布 | 状态 completed，审批数为 2 |
| SCN-010 | 查询不存在模板/运行 | HTTP 404 |

## 4. 自动化命令

```bash
PYTHONPATH=backend python -m unittest discover -s backend/tests -v
npm run build --prefix apps/web
python -m compileall -q backend
docker compose config --quiet
```

## 5. 生产验收待办

- 对 Airflow/DolphinScheduler、Prometheus/Loki、GitLab/Jenkins、Rundeck/Ansible 至少各完成一个真实 Adapter 契约测试。
- 对 Token 过期、接口超时、重复回调、网络分区、部分成功和幂等重试做故障注入。
- 高风险审批校验审批人权限、双人审批、超时撤销和审批后输入漂移。
- 场景运行迁移到 PostgreSQL/Temporal，验证服务重启后恢复和多实例并发一致性。
- 所有真实执行动作验证白名单、最小权限、审计不可抵赖和回滚路径。

当前实现用于产品流程和 API 联调，外部系统动作仍是明确标记的演示证据，不能作为生产系统已执行真实处置的证明。
