# Aegis AI 知识库模块产品与研发设计方案

版本：v1.3
日期：2026-08-20

## 1. 设计依据与目标

本模块参考 LangChain Retrieval/RAG 的组件边界：Document Loader 统一来源，Text Splitter 切分片段，Embedding 与 Vector Store 建索引，Retriever 召回上下文，再由模型生成有依据的回答。LangChain 将可预测的 2-Step RAG、灵活的 Agentic RAG 和带校验的 Hybrid RAG 分开描述。

当前代码采用 LangChain 的模块化 Text Splitter：`langchain-text-splitters>=0.3.11,<0.4`，兼容 Python 3.9。完整 LangChain 不是运行时强制依赖；安装失败或完全离线环境会自动使用内置递归分块器，并在知识库配置中显示实际分块器。这样既借鉴开源组件的稳定实现，也保留企业本地部署的可用性。

### 1.1 开源参考与使用边界

| 项目与调研版本 | 许可证 | 本项目用法 | 代码复用边界 |
|---|---|---|---|
| LangChain `85602c3` | MIT | 直接依赖独立的 Text Splitters 包，实现递归分块和 Markdown 标题分块 | 通过 Python 包 API 使用，保留依赖许可证 |
| RAGFlow `c90a0b7` | Apache-2.0 | 借鉴文档解析状态、分块核验、相似度阈值与检索测试工作流 | 当前界面、模型和接口均重新实现，未复制其组件源码 |
| Dify `98c2ffe` | 修改版 Apache-2.0，含多租户、Logo 和外观设计限制 | 仅对照数据集命中测试、元数据过滤等产品概念 | 不复制前端、交互外观或源码 |
| FastGPT `40d0713` | 带额外条件的开源许可证 | 仅对照知识库创建、分块参数和单点检索测试流程 | 不复制前端、交互外观或源码 |

开源项目的版本、许可证和引用位置均使用 2026-08-20 调研时的 GitHub HEAD；升级或引入新代码前必须重新执行许可证扫描。Aegis 新增的相似度阈值、文档治理和纵向操作界面是独立实现，不形成对第三方前端作品的派生复制。

Aegis 的知识库不是孤立聊天页，而是可供智能问数、AIOps、SQL 优化和场景中心复用的企业上下文底座。用户在一个清晰的任务视图组内完成：

    选择/创建知识库 → 添加资料 → 检查状态/分块 → 调整参数 → 重建索引 → 检索测试 → 引用问答

当前实现是完全本地、可离线运行的 MVP，采用与 LangChain 相同的模块化边界，并实现混合词法检索、标签过滤、多文档去重和引用校验。模型网关可选，未配置时抽取式回答自动降级。后续可无缝替换为 Loader、Embedding、Vector Store、Reranker 和 Model Gateway。

## 2. 产品设计

### 2.1 用户与对象

| 角色 | 任务 | 默认权限 |
|---|---|---|
| 业务用户 | 查询制度、指标口径和手册 | 检索与查看引用 |
| 数据工程师/DBA | 沉淀 TiDB、SQL、数据质量资料 | 新建库、入库、重建索引 |
| SRE | 检索 Runbook、故障案例和变更规范 | 检索、引用、转任务 |
| 知识管理员 | 维护分类、连接器和质量 | 管理工作区知识库 |
| 安全/审计 | 检查数据范围、引用和模型使用 | 只读审计 |

- KnowledgeBase：知识空间、权限范围、分块和检索策略。
- KnowledgeDocument：来源可追踪的资料，记录版本、来源、标签和状态。
- KnowledgeChunk：可独立召回的片段，带位置和元数据。
- KnowledgeQuery/Citation：问题、检索策略、回答、文档/片段/相关度来源。

### 2.2 页面与流程

    ┌─ 知识库列表 ─────┬─ 当前知识库概览 ───────────────────────────┐
    │ + 新建知识库     │ 文档数 / 片段数 / 分块大小 / 索引模式      │
    │ 企业运营知识库   ├─ 检索问答 ─────────────────────────────────┤
    │ 产品文档库       │ 问题输入                         [检索回答] │
    │ 数据治理知识库   │ 回答、置信度、查询 ID、引用片段与相关度     │
    │                  ├─ 文档库 ─────────────┬─ 添加资料 ──────────┤
    │                  │ 文档/来源/状态/片段数│ 文本/文件/本机目录  │
    └──────────────────┴──────────────────────┴─────────────────────┘

核心交互：

1. 进入页面自动选择最近知识库；切换库时清空上一轮回答，防止串库。
2. 新建只要求名称，默认本地索引；管理员页再配置 Embedding、分块和策略。
3. 文本入库要求标题和正文；文件支持 TXT、Markdown、HTML、JSON、SQL/DDL。
4. 本机目录仅本地部署可见，服务端按允许根目录扫描，拒绝符号链接。
5. 问答返回回答、置信度、检索模式、查询 ID 和最多 5 条引用。
6. 无证据时明确拒答，不让模型自行补全事实。
7. 文档可暂停/恢复参与检索；重新索引使用当前知识库分块配置，删除时同步清理片段。
8. 检索测试可调 Top K、标签和 0-1 相似度阈值，响应回显实际阈值，便于复现实验。

### 2.3 指标与异常

- 入库成功率目标 ≥99.5%，检索 P95 <1 秒。
- Citation Coverage 目标 100%，回答事实必须可被引用支持。
- Retrieval Recall@5 目标 ≥85%，Groundedness 目标 ≥90%。
- 权限泄漏、跨租户召回和未授权引用为 0。
- 失败场景明确返回：格式 415、目录越界 403、文件超限 413、非法配置 422、未知知识库 404。

## 3. 架构规划

    资料来源 → Loader → Parser/清洗 → Splitter → Indexer
                                      ├→ 全文索引
                                      └→ Embedding → Vector Store
    用户问题 → 标签过滤 → Hybrid Retriever → 多样性去重 → ACL Filter → Reranker
             → Answer Composer/Model Gateway → Citation Validator → 回答/审计

| 组件 | 当前 MVP | 生产实现 |
|---|---|---|
| Loader | 文本、Multipart、允许目录 | LangChain Loader Adapter、Wiki/Git/网盘/数据库 |
| Parser | UTF-8、HTML 去脚本/样式 | Tika/Unstructured、PDF/OCR、表格解析 |
| Splitter | LangChain Recursive、Markdown Header；离线时使用内置兼容实现 | 代码/表格感知、父子分块、版本化索引 |
| Retriever | 正文/标题/标签词法 + 中文字符 n-gram 混合相关度、多文档去重 | OpenSearch BM25 + 向量 + RRF + Reranker |
| Generator | 默认优先 Model Gateway，输出必须带 `[n]` 引用；失败降级为抽取式回答 | OpenAI-compatible/自建模型 + JSON Schema + 引用校验 |
| Storage | 进程内存 | PostgreSQL、MinIO、OpenSearch、pgvector/Qdrant/Milvus |
| Workflow | 同步 | Kafka/Temporal IndexJob，幂等、重试、版本切换 |

默认 2-Step RAG 适合制度和手册问答；Hybrid RAG 为生产默认。当前页面支持 Top K、标签过滤、相似度阈值和“只检索/生成回答”切换，响应返回候选数、实际阈值、检索耗时、生成模式、命中词和引用定位。Agentic RAG 只由场景中心按需调用 Retriever Tool，并设置步数、Token 和超时。

生产元数据表建议：

    knowledge_base / knowledge_document / knowledge_chunk
    knowledge_embedding / knowledge_query / knowledge_citation / index_job

每次检索必须把 tenant、workspace、部门、密级和文档 ACL 作为服务端过滤条件下推到索引与向量库，不能先全库召回再交给模型过滤。

Embedding、Rerank、Chat 均通过 Model Gateway Port 适配 OpenAI-compatible、Ollama、vLLM、TGI、Xinference 或企业自建 HTTP/gRPC。知识库仅保存策略标识，密钥放入 Vault/OpenBao； local-only 工作区禁止路由到公网。

安全边界：上传限制扩展名、数量、大小、MIME 和恶意文件扫描；原文存对象存储并通过短期签名 URL 访问；文档内容视为不可信 Prompt，不能覆盖系统指令；日志不记录原文、问题全文、Embedding 或密钥。

## 4. 前端开发设计

页面组件：KnowledgeLibraryList、KnowledgeSummary、KnowledgeTaskPicker、KnowledgeQueryPanel、RetrievalThreshold、CitationList、KnowledgeDocumentList、DocumentActions、ChunkPreview、RetrievalHistory、KnowledgeSettings、KnowledgeIngestPanel。当前代码在 apps/web/src/main.tsx 和 apps/web/src/knowledge.css，复用既有 AppShell、Panel、Chip 和 API helper，保持知识库选择、指标、配置和任务区域纵向展开。

任务视图固定为：

1. **问知识库**：日常用户只看到库选择、场景问题、问题输入、回答和引用。
2. **文档管理**：管理员负责文本/文件/目录入库，并可打开文档查看 Chunk。
3. **检索测试**：数据工程师调整 Top K、标签和相似度阈值，检查召回片段、查询历史和回答反馈。
4. **纵向配置**：管理员直接修改索引模式、分块模式、分块大小和重叠大小；保存时按需重建现有文档。

这一分层吸收 Dify 的创建/预览/配置分步体验、RAGFlow 的 Chunk 人工核验和 FastGPT 的单点检索测试；不把高级参数放入普通问答首屏。

状态模型：

- libraries/selectedId：知识库集合与当前选择。
- documents：当前库文档，切换后重新请求。
- question/result：本轮问题与引用回答，切库清空。
- title/content/tags/directory：文本、标签和目录表单。
- busy：create/text/upload/directory/query 独立防重复提交。
- error：统一错误条，可关闭。

生产版再拆路由级代码块，使用 TanStack Query 缓存知识库与文档，分片上传，SSE 推送 IndexJob，引用支持定位原文和反馈。补齐 label、键盘焦点、aria-live、上传进度和颜色之外的状态标识。

## 5. 后端开发设计

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | /api/v1/knowledge-bases | 查询知识库，支持 search |
| POST | /api/v1/knowledge-bases | 创建知识库 |
| GET | /api/v1/knowledge-bases/{id} | 查看知识库 |
| PATCH | /api/v1/knowledge-bases/{id} | 修改索引、分块模式、大小和重叠并按需重建 |
| GET | /api/v1/knowledge-bases/index-modes | 查询关键词、字符语义和混合索引模式 |
| GET | /api/v1/knowledge-bases/chunking-modes | 查询递归与 Markdown 标题分块模式 |
| GET | /api/v1/knowledge-bases/{id}/documents | 查询文档 |
| POST | /api/v1/knowledge-bases/{id}/documents | 文本入库 |
| POST | /api/v1/knowledge-bases/{id}/documents/upload | 多文件入库 |
| POST | /api/v1/knowledge-bases/{id}/documents/local-directory | 扫描允许目录 |
| POST | /api/v1/knowledge-bases/{id}/query | 检索并返回引用回答 |
| GET | /api/v1/knowledge-bases/{id}/queries | 查询最近检索记录 |
| GET | /api/v1/knowledge-bases/{id}/documents/{document_id}/chunks | 检查文档 Chunk |
| PATCH | /api/v1/knowledge-bases/{id}/documents/{document_id} | 启用或暂停文档检索 |
| POST | /api/v1/knowledge-bases/{id}/documents/{document_id}/reindex | 按当前配置重新索引文档 |
| DELETE | /api/v1/knowledge-bases/{id}/documents/{document_id} | 删除文档、原文缓存和全部 Chunk |
| POST | /api/v1/knowledge-bases/{id}/queries/{query_id}/feedback | 提交回答反馈 |

领域模型、切分和检索位于 backend/app/knowledge_base.py；HTTP、HTML 清洗和文件安全位于 backend/app/main.py。`ChunkingStrategy` 把递归分块和 Markdown 标题分块隔离为稳定契约，后续可以增加代码、表格和父子分块适配器。当前为进程内状态，重启后新增数据丢失，仅适合本地交互验证；查询历史、Chunk 检查、文档治理和反馈接口已经建立生产契约，后续迁移 PostgreSQL 即可持久化。

问答生成有三道门：没有命中证据时拒答；关闭生成时只返回检索结果；配置模型时要求模型输出至少一个且仅能来自本次召回的 `[n]` 引用，否则回退到抽取式引用回答。文档内容被明确标记为不可信证据，不能覆盖系统提示词，防止文档 Prompt Injection。

生产流程：

    文档 → 鉴权/配额/扫描 → MinIO → Outbox → Worker 解析/切分/Embedding/索引
    查询 → 鉴权/ACL → BM25+Vector → Rerank → 结构化生成 → 引用校验 → 审计

IndexJob 使用文档 checksum、parser/splitter/embedding 版本作为幂等键。更新策略时创建新索引版本，验证通过后原子切换别名。

## 6. 部署运维与路线图

本地 Compose 不增加强制依赖，允许目录由只读卷和 DATASET_ALLOWED_ROOTS 控制。生产新增 knowledge-worker、MinIO/S3、OpenSearch、pgvector/Qdrant/Milvus、本地 Embedding/Reranker 服务；监控入库队列、解析/Embedding 失败、召回延迟、无结果率、Citation Coverage 和索引版本。

| 阶段 | 交付 | 退出条件 |
|---|---|---|
| 已完成 MVP | 知识库、文本/文件/目录入库、递归/Markdown 分块、文档启停/重建/删除、可调阈值检索、引用定位和模型降级 | 自动化和构建通过 |
| Sprint 1 | PostgreSQL、MinIO、IndexJob、版本/删除/重试 | 重启不丢数据，任务可恢复 |
| Sprint 2 | OpenSearch、本地 Embedding、Hybrid Retriever | Recall@5 达基线 |
| Sprint 3 | Model Gateway、Reranker、引用校验和 ACL | 无越权，Citation Coverage 100% |
| Sprint 4 | Wiki/Git/网盘连接器、增量同步、Agent Tool | 三类生产来源稳定同步 |

参考：

- LangChain Retrieval：https://docs.langchain.com/oss/python/langchain/retrieval
- LangChain Knowledge Base：https://docs.langchain.com/oss/python/langchain/knowledge-base
- LangChain Document Loaders：https://docs.langchain.com/oss/python/integrations/document_loaders
- LangChain Text Splitters：https://github.com/langchain-ai/langchain/tree/master/libs/text-splitters（MIT）
- RAGFlow Similarity Slider：https://github.com/infiniflow/ragflow/blob/main/web/src/components/similarity-slider/index.tsx（Apache-2.0，仅借鉴参数与测试工作流）
- RAGFlow Document Pipeline：https://github.com/infiniflow/ragflow/tree/main/web/src/components/document-pipeline-dialog（Apache-2.0，仅借鉴状态和治理流程）
- Dify Hit Testing：https://github.com/langgenius/dify/blob/main/api/controllers/console/datasets/hit_testing_base.py（修改版许可证，仅产品对照）
- FastGPT Dataset：https://github.com/labring/FastGPT/tree/main/projects/app/src/pageComponents/dataset（带额外条件的许可证，仅产品对照）
