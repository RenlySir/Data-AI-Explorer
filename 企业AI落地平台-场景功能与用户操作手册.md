# Aegis AI 场景功能与用户操作手册

版本：v1.0
日期：2026-08-19
适用范围：当前本地 MVP 与后续生产版

## 1. 手册目的

本手册按“我要完成什么工作”组织功能，不要求用户先理解 Text2SQL、RAG、Agent 或执行计划。用户登录后可以从工作台选择一个任务，进入对应页面，提交必要输入，核验结果，再决定是否转任务、审批或执行。

状态说明：

- **已实现**：当前本地版本可以直接操作和验证。
- **演示闭环**：页面、接口和状态流已实现，外部系统调用使用确定性演示数据。
- **生产接入**：架构和接口已规划，需连接企业数据源、权限、模型或执行平台后验收。

## 2. 登录后的最短路径

```text
登录
  → 工作台选择“经营数据分析 / 查询企业知识 / 诊断 SQL 性能 / 发起协同任务”
  → 在目标页面选择对象或输入问题
  → 运行分析
  → 核验 SQL、引用、执行计划、证据或审批状态
  → 保存结果、继续追问、转任务或审批执行
```

左侧导航按用户任务排序：

```text
工作台
├─ 数据分析：智能问数、知识库、数据资产、数据关系
├─ 运维治理：AIOps 事件、SQL 优化
└─ 协同执行：场景中心
```

工作台只承载待办、运营摘要和高频任务入口；复杂配置留在对应模块的“配置”视图或管理中心。

## 3. 场景到功能映射

| 用户场景 | 入口模块 | 具体功能 | 最少输入 | 主要结果 | 当前状态 |
|---|---|---|---|---|---|
| 查询经营指标和趋势 | 智能问数 | 自然语言问数 | 一句话问题 | SQL、数据表、ECharts 图表、证据 | 已实现 |
| 分析 CSV/Parquet 文件 | 智能问数 | 文件数据集分析 | 文件、问题 | 聚合结果和图表 | 已实现 CSV；Parquet 生产接入 |
| 扫描服务器数据目录 | 智能问数 | 本机目录采集 | 允许目录、问题 | 数据集列表和关联分析 | 已实现受控目录演示 |
| 梳理数据库关系 | 数据关系 | 元数据与关联 SQL 采集 | 已登记 TiDB/MySQL 数据源 | Schema、表、字段 Comment、表级/字段级网络 | 已实现可操作闭环 |
| 查制度、手册、故障案例 | 知识库 | 有引用问答 | 知识库、问题 | 回答、引用片段、相关度、反馈 | 已实现 |
| 维护企业资料 | 知识库 | 文档管理 | 文本、文件或允许目录 | 文档、索引状态、Chunk 预览 | 已实现 |
| 调试召回质量 | 知识库 | 检索测试 | 问题 | 命中片段、历史查询和引用 | 已实现 |
| 诊断 TiDB 慢 SQL | SQL 优化 | 版本化计划模拟 | SQL、DDL、TiDB 版本 | 执行计划、风险、索引/SQL 建议 | 已实现模拟模式 |
| 使用真实 TiDB 计划 | SQL 优化 | 在线 EXPLAIN | SQL、DDL、TiDB 连接 | 真实计划和基于版本的建议 | 生产接入 |
| 处理生产告警 | AIOps 事件 | Incident 调查 | 事件 | 影响、证据、根因候选、处置入口 | 演示闭环 |
| 夜间跑批值守 | 场景中心 | 跑批值守模板 | 目标、任务上下文 | 调查步骤、重跑门禁、校验结果 | 演示闭环 |
| 生产故障协同 | 场景中心 | 虚拟作战室模板 | 故障目标、影响上下文 | 并行证据、止损建议、审批、验证 | 演示闭环 |
| 数据质量异常调查 | 场景中心 | 质量调查模板 | 失败规则、数据范围 | 血缘影响、修复计划、复验 | 演示闭环 |
| 发布风险评审与观察 | 场景中心 | 发布前评审/发布后观察 | 版本、变更上下文 | 风险评分、阻断项、观察结论 | 演示闭环 |
| 客户工单诊断 | 场景中心 | 工单诊断模板 | 工单与授权范围 | 分类、知识引用、回复草稿 | 演示闭环 |
| 漏洞响应、FinOps、项目周报 | 场景中心 | 对应场景模板 | 任务目标、上下文 | 分步结果、证据、审批和审计 | 演示闭环 |

## 4. 智能问数使用方法

### 4.1 TiDB 自然语言问数

适用：经营分析师、数据分析师、管理者查询销售、订单、客户和运营指标。

1. 点击工作台“经营数据分析”或左侧“智能问数”。
2. 确认顶部数据连接；首次使用点击“采集结构”，系统通过 TiDB MCP 获取 Schema、表、字段和 Comment。
3. 输入业务问题，例如“近 30 天各区域 GMV 趋势如何”。
4. 点击“开始分析”。系统完成语义匹配、只读 SQL 生成、AST 安全检查、执行和结果校验。
5. 先看业务结论和图表，再核验 SQL、数据行和使用的数据资产。
6. 问题口径不明确时，先选择时间、指标口径或维度，再继续执行。

完成标准：结果包含可读结论、图表、只读 SQL、数据来源和口径证据。不得只返回模型文字。

### 4.2 文件与目录分析

1. 在智能问数页选择“上传数据集”或“扫描目录”。
2. 上传 CSV/Parquet，或输入部署端允许访问的目录；浏览器不能直接读取任意客户端目录。
3. 选择已登记的数据集并输入问题。
4. 点击分析，核验字段类型、过滤条件、聚合维度和图表。

安全约束：目录必须位于服务端白名单根目录；拒绝符号链接越界；文件限制类型、大小和数量；敏感数据不发送到未经批准的公网模型。

## 5. 知识库使用方法

知识库采用四个任务视图，避免在同一页面堆放全部控件。

### 5.1 问知识库

1. 从左侧知识库列表选择范围正确的知识库。
2. 可点击“TiDB 慢 SQL、生产变更、数据质量、故障值班”等场景问题，也可直接输入问题。
3. 点击“检索并回答”。
4. 阅读回答并展开引用，核验文档、片段和相关度。
5. 点击“有帮助”或“需改进”，反馈用于后续检索评测。

无可靠证据时系统必须明确拒答。切换知识库后自动清空上一库回答，防止串库。

### 5.2 文档管理

1. 进入“文档管理”。
2. 选择文本录入、文件上传或本机允许目录。
3. 填写标题、标签和正文并提交。
4. 等待文档状态为就绪；点击文档查看 Chunk 切分结果。
5. 若标题、表格、代码或上下文被错误切开，调整分块参数后重建索引。

### 5.3 检索测试

1. 进入“检索测试”，输入一个真实业务问题。
2. 运行测试并检查召回片段和引用是否包含正确答案。
3. 对照最近查询历史，观察相似问题是否稳定召回。
4. 将失败问题加入评测集，再调整分块、检索或重排策略。

### 5.4 配置

配置页展示知识库范围、检索策略、模型提供方、Chunk 大小和重叠。生产环境切换模型或索引策略前必须离线评测、灰度验证并保留可回滚索引版本。

## 6. SQL 优化使用方法

适用：DBA、开发和 SRE 分析单条或批量 TiDB SQL。

1. 点击工作台“诊断 SQL 性能”或左侧“SQL 优化”。
2. 选择 TiDB 版本；版本画像只使用该版本可用的优化器特性。
3. 粘贴 SQL 和 DDL，或上传 `.sql` 文件/读取允许目录。
4. 选择“模拟模式”快速检查；接入目标集群后可选择“真实 EXPLAIN”。
5. 点击“开始优化”，依次核验执行计划、风险节点、依据、改写 SQL 和索引建议。
6. 在测试环境验证建议的收益、写放大和存储成本后，再走 DDL 变更审批。

边界：模拟计划不是 TiDB 真实优化器输出；真实计划只能使用只读 EXPLAIN；系统不会自动在生产创建索引或改写数据。

## 7. AIOps 与场景中心使用方法

### 7.1 Incident 调查

1. 从工作台“需要关注”或左侧“AIOps 事件”进入事件。
2. 先确认严重级别、影响服务、时间范围和负责人。
3. 查看指标、日志、链路、数据库和最近变更证据。
4. 比较根因候选与反证，再选择受控 Runbook。
5. 高风险动作提交审批；执行后按 SLO 和业务指标验证恢复。
6. 关闭前生成复盘，并把确认结论沉淀到知识库。

### 7.2 场景任务

1. 点击工作台“发起协同任务”或左侧“场景中心”。
2. 按类别选择模板，先阅读目标、集成、指标和审批策略。
3. 填写本次任务目标和上下文，点击启动。
4. 在运行面板按顺序推进 Agent 步骤，检查每一步证据。
5. 状态为“等待审批”时，由有权限的人员只批准当前动作。
6. 全部步骤完成后核验业务结果和审计轨迹。

高风险操作包括重启、切流、数据修复、补数、发布、生产补丁和对外回复。生产版必须使用白名单 Runbook、短期凭证、幂等键、回滚和二次验证。

## 8. 十二个场景的具体落点

| 场景模板 | 主要使用页面/功能 | 用户操作重点 |
|---|---|---|
| 夜间跑批值守与自动恢复 | 场景中心 + AIOps + 知识库 | 选择失败任务，核验分类证据，按策略重跑，复验数据 |
| 生产故障虚拟作战室 | AIOps 事件 + 场景中心 | 建立根任务，并行调查，审批止损，验证恢复 |
| 脚本异常自动修复闭环 | 场景中心 | 提取报错，创建分支补丁，隔离测试，Review 后发布 |
| 数据库每日健康巡检 | 场景中心 + SQL 优化 | 检查慢 SQL/锁/容量/备份，生成异常任务和日报 |
| 发布前自动风险评审 | 场景中心 | 输入版本，核验代码/依赖/DDL/安全/回滚阻断项 |
| 发布后智能观察窗 | 场景中心 + AIOps | 建立发布前基线，持续比较，决定关闭或回滚 |
| 数据质量异常调查 | 场景中心 + 数据资产 + 数据关系 | 选择失败规则，追溯血缘，审批修复，复跑质量规则 |
| 企业智能问数交付团队 | 智能问数 + 数据关系 | 确认口径，生成只读 SQL，复核数量级，生成图表 |
| 客户工单自动诊断 | 场景中心 + 知识库 | 分类定级，检索引用，检查授权环境，人工确认回复 |
| 安全漏洞响应流水线 | 场景中心 + 数据资产 | 同步 CVE，匹配资产，测试补丁，分批修复和复扫 |
| 云成本持续优化 | 场景中心 | 识别闲置，测算影响，生成变更任务，追踪实际节省 |
| 项目数字参谋部 | 场景中心 | 汇总 Git/CI/任务事实，识别阻塞，生成报告和任务草稿 |

## 9. 用户体验与页面验收规则

1. 每个页面只突出一个当前主操作；次要设置放到对应 Tab 或详情区。
2. 所有列表行都能进入对象或执行明确动作，不使用纯装饰卡片。
3. 结果页同时给出结论和证据；SQL、引用、计划、日志和审批不能被隐藏到另一个系统。
4. 空态说明当前缺少什么，并提供“创建、上传、连接或查看演示数据”按钮。
5. 加载、空数据、无权限、超时、部分成功、错误重试和只读降级都有页面状态。
6. 桌面端适合持续工作；移动端保证查看告警、回答、任务进度和审批，不要求编辑复杂 SQL。
7. 生产、测试环境持续可见；危险动作显示目标、影响、回滚和审批人。
8. 页面不暴露模型密钥、数据库密码、原文敏感内容或内部思维链。

## 10. 管理员上线清单

1. 配置租户、工作区、RBAC/ABAC、数据行列权限和审计留存。
2. 接入 TiDB MCP、只读查询账户、元数据采集和查询限额。
3. 配置 Model Gateway，并标注本地、公网、数据级别和模型能力。
4. 配置知识库对象存储、索引、Embedding、重排和评测集。
5. 接入 Prometheus、日志、链路、调度、Git、CI 和执行平台 Adapter。
6. 注册白名单 Runbook、参数 Schema、审批策略、验证和回滚。
7. 按专项测试报告完成安全、权限、准确性、恢复、性能和离线部署验收。
