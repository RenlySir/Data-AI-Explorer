# 多场景中心开发说明

## 1. 来源与目标

本模块依据飞书文档[《多场景探索》](https://pingcap-cn.feishu.cn/wiki/SRmBwpk7QiaXZFkM9bYciwWfnJe)设计。文档对企业 AI 的定位不是单一聊天工具，而是连接监控、数据库、调度、代码库、执行平台与人工责任链的“AI 协作与控制平面”。

场景中心将文档中的 12 个场景统一抽象为：

```text
场景模板 → 运行实例 → Agent 步骤 → 工具动作 → 证据 → 审批 → 结果
```

这样可以复用触发器、任务、Agent、Runbook、审批、审计和指标能力，避免每个业务场景单独建设一套流程引擎。

## 2. 已实现场景

| 类别 | 场景 | 关键能力 |
|---|---|---|
| AIOps 运维 | 夜间跑批值守与自动恢复 | 调度检查、日志分类、策略重跑、数据校验 |
| AIOps 运维 | 生产故障虚拟作战室 | 根任务、并行调查、止损审批、恢复验证 |
| 研发交付 | 脚本异常自动修复闭环 | Git 分支、补丁、隔离测试、Review、发布审批 |
| 数据与数据库 | 数据库每日健康巡检 | 慢 SQL、锁、连接、容量、备份、SQL 优化 |
| 研发交付 | 发布前自动风险评审 | Diff、测试、安全、DDL、回滚和发布评分 |
| 研发交付 | 发布后智能观察窗 | 基线对比、持续观测、关闭或回滚建议 |
| 数据与数据库 | 数据质量异常调查 | 质量规则、血缘、影响分析、修复和复验 |
| 数据分析 | 企业智能问数交付团队 | 口径、Text2SQL、验证和 ECharts 报表 |
| 客户支持 | 客户工单自动诊断 | 分类、知识检索、授权诊断和回复审批 |
| 安全治理 | 安全漏洞响应流水线 | CVE、SBOM、资产定位、补丁和复扫 |
| 成本治理 | 云成本持续优化 | 闲置识别、节省测算、变更建议和收益追踪 |
| 项目协作 | 项目数字参谋部 | Git/CI/任务汇总、阻塞识别和日报周报 |

## 3. 产品页面

一级导航新增“场景中心”。页面由四部分组成：

1. **场景模板库**：按类别筛选 12 个模板，展示价值、Agent 数量和步骤数。
2. **场景详情**：展示 Agent 阵容、触发器、连接系统、审批策略和执行步骤。
3. **任务启动器**：用户填写目标和上下文后创建一次运行实例。
4. **运行指挥面板**：展示步骤、进度、证据、审批状态和审计轨迹。

普通用户只需按“选择模板 → 填写目标 → 启动场景 → 推进/审批”的页面顺序即可完成一次流程。

## 4. 后端模型

### 4.1 ScenarioTemplate

模板包含：`id`、名称、类别、描述、价值、Agent 阵容、触发器、外部集成、审批策略、指标和步骤。模板当前以代码版本化，后续迁移到 PostgreSQL 后仍保持同一 API 契约。

### 4.2 ScenarioRun

运行实例保存：任务目标、上下文、状态、当前步骤、全部步骤、审批计数和审计日志。状态包括：

- `running`：当前步骤可执行。
- `waiting_approval`：高风险动作被阻断。
- `completed`：全部步骤完成。
- `failed`：执行失败，等待人工处置或重试。

### 4.3 风险门禁

步骤按 `low / medium / high` 分级。当前 MVP 中：

- low：只读查询、证据采集和报告生成，可直接推进。
- medium：受控 Runbook 或产生任务草稿，可由策略决定是否审批。
- high：发布、重启、切流、修复 SQL、补数、外发回复、生产补丁等，必须进入 `waiting_approval`。

未审批时重复推进返回 HTTP 409；审批只对当前步骤生效，不能复用于后续高风险步骤。

## 5. API

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/v1/scenarios` | 查询场景，可按类别和状态筛选 |
| GET | `/api/v1/scenarios/{id}` | 查看模板详情 |
| POST | `/api/v1/scenarios/{id}/runs` | 创建运行实例 |
| GET | `/api/v1/scenario-runs` | 查询运行记录 |
| GET | `/api/v1/scenario-runs/{id}` | 查看运行详情 |
| POST | `/api/v1/scenario-runs/{id}/advance` | 推进一个步骤 |
| POST | `/api/v1/scenario-runs/{id}/approve` | 审批当前高风险步骤 |

## 6. 外部系统适配

场景模板已声明目标集成，但当前本地 MVP 不会伪造 Prometheus、Airflow、GitLab 等生产调用；推进步骤产生的是明确标记的演示执行证据。生产接入采用 Adapter SPI：

```text
Scenario Orchestrator
  ├─ Observability Adapter: Prometheus / Loki / OpenSearch / Tempo
  ├─ Scheduler Adapter: Airflow / DolphinScheduler / Argo Workflows
  ├─ Database Adapter: TiDB MCP / Dashboard / SQL Optimizer
  ├─ Delivery Adapter: GitLab / Jenkins / Argo Rollouts
  ├─ Security Adapter: Trivy / Dependency-Track / Wazuh
  └─ Execution Adapter: Rundeck / StackStorm / Ansible
```

每个 Adapter 需要实现连接测试、只读查询、动作 Schema、权限声明、超时、幂等键、审计字段和结果标准化。生产动作必须通过命令白名单和短期凭证执行。

## 7. 实施顺序

1. 第一阶段：只读拉取、分析、任务和报告。
2. 第二阶段：开放任务重试、Pod 重启等低风险 Runbook。
3. 第三阶段：Git 分支补丁、测试、Review 和审批发布。
4. 第四阶段：仅对历史成功率高、边界明确、可快速回滚的动作开放有限自治。

首批生产 PoC 建议选择夜间跑批值守、数据库健康巡检和发布前风险评审。它们输入输出清晰、风险可控，也容易衡量节省工时、自动恢复率和评审时长。
