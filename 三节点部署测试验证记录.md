# 三节点部署测试验证记录

## 1. 测试范围

本记录对应 `three-node-demo-20260820`，验证三台既有 TiDB 节点上的 Aegis API、控制节点静态 Web、TiDB 直连问数、关系采集、实时 SQL 优化，以及知识库、AIOps、场景中心等演示入口。平台设置和审计元数据写入独立的 TiDB `aegis_platform` 数据库。

| 节点 | 角色 | API | Web | TiDB SQL | TiDB Status |
| --- | --- | --- | --- | --- | --- |
| `10.2.106.5` | Control | `18082` | `18081` | `4100` | `11080` |
| `10.2.106.124` | Worker-AI | `18082` | - | `4100` | `11080` |
| `10.2.106.182` | Worker-Ops | `18082` | - | `4100` | `11080` |

三台节点实际 TiDB 状态接口返回 `8.0.11-TiDB-v9.1.0`；部署过程未停止或覆盖既有 `/opt/tidb-v91`。

## 2. 已执行验证

| 用例 | 操作 | 结果 |
| --- | --- | --- |
| 节点健康 | 三台 `GET /health` | 通过；角色分别为 `control`、`worker-ai`、`worker-ops` |
| 集群发现 | 三台 `GET /api/v1/deployment/status` | 通过；3 个 TiDB endpoint 均 `ready`，8 个产品模块均已装配 |
| Web 服务 | `GET http://10.2.106.5:18081/` | 通过；Python 3.6 静态服务器返回 Vite 页面 |
| 演示数据 | `scripts/seed_tidb_demo.py` | 通过；`aegis_demo` 写入 3 个客户、30 个订单、3 个事件，并执行 `ANALYZE TABLE` |
| 数据源接入 | 创建 TiDB 数据源并连接测试 | 通过；发现 16 张表 |
| ChatBI | 自然语言问数、TiDB 执行、图表生成、加入大屏 | 通过；返回 line/bar 图表和只读 SQL 证据 |
| 数据关系 | 全量 Schema/表/字段 Comment 采集 | 通过；5 个 Schema、76 个节点、71 条结构边 |
| SQL 关系 | 提交真实 JOIN SQL 观察 | 通过；生成 2 条字段/表关系边，并记录 SQL Digest |
| SQL 优化模拟 | TiDB `8.5` 版本 + SQL/DDL | 通过；返回模拟计划、版本特性和 SQLAdvisor 风格建议 |
| SQL 优化 live | TiDB `9.1` + `EXPLAIN FORMAT='verbose'` | 通过；三台 API 均返回真实计划，实际版本为 `8.0.11-TiDB-v9.1.0` |
| 知识库 | Markdown 文档、Markdown 分块、混合检索、引用回答 | 通过；返回检索片段和引用 |
| AIOps | 事件、资产列表接口 | 通过；演示数据可查询 |
| 场景中心 | 创建智能问数场景、推进步骤 | 通过；状态由 `running` 推进到下一步骤 |
| 平台登录与设置 | 登录、修改设置、重启控制 API、跨节点读取设置 | 通过；`platform_settings` 从 TiDB 恢复，三节点返回一致配置 |
| 平台审计与治理任务 | 登录/设置审计、资产治理任务、事件领取/已读 | 通过；API 返回审计动作和治理任务草稿 |
| 问数操作流 | ChatBI 查询、SSE 阶段事件、请求 ID、安全响应头、Prometheus 指标 | 通过；返回 `PLANNING`、`VALIDATING`、`EXECUTING`、`COMPLETED`，`/metrics` 可采集 |

## 3. 重复验证命令

```bash
./scripts/verify-three-node.sh
curl http://10.2.106.5:18081/
curl http://10.2.106.5:18082/docs
```

重新发布时，`scripts/deploy-three-node.sh` 会在本地构建 `apps/web/dist`，通过 SSH 传到控制节点，三台主机使用 Python 3.9 venv 安装后端依赖并由 systemd 拉起服务。Python 3.9 环境将 MCP SDK 作为可选依赖；未接入 MCP 时，TiDB 直连元数据/问数和受控 live EXPLAIN 仍可演示。平台元数据可通过 `scripts/migrate_platform_tidb.py` 显式初始化，API 启动也会自动执行幂等初始化。

## 4. 验收边界

- 当前演示数据源、知识库、报表、关系缓存和场景运行状态仍保存在 API 进程内存中，服务重启后需要重新创建或重新采集；工作空间设置和审计事件已持久化至 TiDB `aegis_platform`。生产需继续把业务状态、对象文件和工作流检查点接入 TiDB/对象存储/任务队列。
- 当前演示不依赖独立向量数据库；知识库保留本地降级检索路径。生产若启用语义检索，优先评估 TiDB 向量索引，其次再引入 OpenSearch/Milvus，并保持检索接口不变。
- 前端工程门禁已启用 ESLint、TypeScript strict、Prettier 和 GitHub Actions；后端回归为 52 个测试用例，仍需在生产 Spring Boot/Worker 迁移完成后补充 Testcontainers、Playwright 和压力测试。
- `external_adapter_mode=demo` 表示 AIOps/场景外部系统 Adapter 使用演示数据，节点已有 node_exporter 可供后续接入 Prometheus；没有虚构为已经接入生产监控、工单或执行器。
- 生产环境必须使用最小权限 TiDB 账号、SSH key、TLS、精确 CORS 和 Vault/OpenBao，禁止沿用演示 root 账号或空密码。
