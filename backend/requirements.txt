fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
# Required for Pydantic to evaluate PEP 604 annotations on Python 3.9.
eval-type-backport>=0.2,<1
httpx>=0.27,<1
python-multipart>=0.0.9,<1
duckdb>=1.1,<2
pandas>=2.2,<3
pyarrow>=16,<22
sqlglot>=25,<30
# MCP introspection is optional on Python 3.9 deployments.  The current MCP
# SDK requires Python 3.10+, while TiDB direct-connect covers the demo path.
mcp>=2,<3; python_version >= "3.10"
pymysql>=1.1,<2
# Optional-compatible RAG splitter used by the knowledge base; the application
# keeps a built-in recursive fallback for offline/minimal installations.
langchain-text-splitters>=0.3.11,<0.4
