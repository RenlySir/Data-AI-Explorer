# 知识库模块专项测试方案与执行报告

版本：v1.2
执行日期：2026-08-20

## 1. 目标与范围

验证“创建知识库 → 文档入库 → 检查状态/分块 → 调参重建 → 检索测试 → 返回引用 → 反馈/评测”的闭环，以及文本、文件、本机目录、Chunk 检查、查询历史、空库、非法配置和未知资源边界。新增验证 LangChain 递归/Markdown 标题分块、内置降级、文档启停/重建/删除、相似度阈值、标签过滤、Top K、多样性去重、仅检索模式和模型回答引用校验。

| 维度 | 内容 |
|---|---|
| 产品功能 | 列表/创建、纵向配置、三个任务视图、三种入库、文档治理、Chunk 预览、检索回答、历史、引用与反馈 |
| API 契约 | 状态码、字段、默认值、参数校验、资源不存在 |
| 文档处理 | UTF-8、Markdown/TXT/HTML/JSON/SQL、递归/Markdown 标题分块、重叠、启停、重建和删除 |
| 检索质量 | 标题/标签/正文命中、Top K、相似度阈值、暂停文档隔离、无结果降级 |
| 安全 | 扩展名、大小、数量、目录 allowlist、符号链接、HTML 脚本忽略 |
| 前端 | TypeScript、Vite 构建、页面加载、入库、问答、响应式 |
| 回归 | 智能问数、SQL 优化、场景中心既有测试 |

## 2. 自动化用例

backend/tests/test_knowledge_base.py 覆盖：

1. 演示知识库和 3 份文档可访问。
2. 新建库、文本入库、问题命中并返回 Citation。
3. Markdown 上传成功，EXE 扩展名返回 415。
4. 允许目录扫描成功，越界目录返回 403。
5. 空库返回低置信度且无引用，非法分块返回 422，未知资源返回 404。
6. 文档 Chunk 可按知识库和文档查询，跨库/未知文档不能读取。
7. 查询进入当前知识库历史，回答反馈绑定合法查询 ID 并可追踪。
8. 混合检索返回候选数、耗时、命中词和检索原因；标签过滤不会召回其他标签文档。
9. 仅检索模式不调用模型；模型输出缺少合法 `[n]` 引用时自动降级为抽取式回答。
10. 分块模式目录返回递归和 Markdown 标题分块；Markdown 章节可产生独立片段。
11. 文档暂停后不参与召回，恢复后重新进入检索候选。
12. 单文档重新索引保留文档身份并刷新片段；删除同步更新知识库文档数和片段数。
13. `score_threshold` 在 0-1 范围内生效并在响应中回显，高阈值可稳定过滤低相关结果。

## 3. 执行命令

    cd backend
    python -m unittest discover -s tests -v
    cd ../apps/web
    npm run build
    cd ../..
    python3 -m compileall -q backend
    docker compose config --quiet
    git diff --check

## 4. 当前执行结果

| 检查 | 结果 |
|---|---|
| 后端全量单元/接口测试 | 51/51 通过（知识库专项 13 项，含 LangChain splitter 实际路径） |
| Python 编译检查 | 通过 |
| TypeScript + Vite 生产构建 | 通过 |
| Compose 配置 | 通过 |
| API 冒烟 | 通过，演示库与引用问答返回正确 |
| 浏览器主流程与截图 | 通过，含纵向配置、三个任务视图、文档暂停/恢复/重建、阈值回显、引用、桌面与 390px |
| 浏览器控制台 | 0 个 error/warning |
| 390px 布局 | body 无横向溢出；文档状态与操作按钮无重叠；配置输入和任务选择无文字裁切 |

前端构建存在 ECharts 产物超过 500 kB 的非阻断警告；生产应对图表和 SQL 编辑器做路由级动态加载。

## 5. 生产级补充测试

- 质量集：200 个真实问题，标注片段，计算 Recall@5、MRR、nDCG。
- 生成集：事实正确性、Groundedness、Citation 完整性和拒答正确率。
- 权限矩阵：租户、工作区、部门、文档 ACL 和密级组合，禁止跨域召回。
- Prompt Injection：文档指令、恶意 URL、数据外传请求和工具诱导均无效。
- 文件安全：病毒、宏、压缩炸弹、畸形 PDF、OCR 超时、编码异常和超大文档。
- 一致性：更新/删除/重建索引、重复事件、Worker 崩溃与恢复、索引别名切换。
- 性能：100 万/1000 万 Chunk 并发、P95、缓存命中和索引吞吐。
- 容灾：PostgreSQL、对象存储、全文与向量索引备份恢复和 checksum 对账。

## 6. 边界与退出标准

当前实现是本地 MVP：数据在内存中，检索为本地混合模式，回答可调用已接入模型并经过引用校验，失败时使用确定性抽取式编排。它证明了产品、API 和页面闭环，不等同于生产 RAG。

生产退出条件：持久化与异步索引可恢复；权限过滤在召回前完成；Recall@5、Groundedness 与 Citation Coverage 达标；外部/本地模型路由通过；上传安全、容量、备份恢复、审计和跨租户测试全部通过。
