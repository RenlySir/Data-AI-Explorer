# 企业 AI 落地平台产品设计方案

版本：v1.0（产品基线）
日期：2026-08-18
产品代号：**Aegis AI Control Plane**

本文是产品层基线；技术实现以[详细架构设计方案](企业AI落地平台-详细架构设计方案.md)为准，研发分工分别参考[前端开发设计方案](企业AI落地平台-前端开发设计方案.md)、[后端开发设计方案](企业AI落地平台-后端开发设计方案.md)和[部署运维设计方案](企业AI落地平台-部署运维设计方案.md)。

## 1. 产品定位

Aegis 是面向企业数据与运维团队的 B/S 架构 AI 控制平面：把自然语言分析、数据关系治理、AIOps 诊断、自愈执行和多智能体协作放在同一套权限、审批、审计和知识体系中。

### 1.1 核心价值

| 目标 | 用户收益 | 可量化指标 |
|---|---|---|
| 让数据可问 | 业务人员用自然语言获得可信指标和图表 | 首次回答成功率、口径争议率 |
| 让故障可处置 | 从告警到根因、Runbook、验证形成闭环 | MTTR、自动处置率 |
| 让关系可治理 | 看清表、SQL、指标、任务、服务之间的依赖 | 血缘覆盖率、重复 SQL 下降 |
| 让决策可追溯 | AI 给建议，人按风险审批，所有证据留痕 | 审批时长、决策采纳率、审计完整率 |

### 1.2 设计原则

1. 可信优先：回答必须显示数据来源、口径、SQL、时间范围和置信度。
2. 最小权限：数据、工具、命令和 Agent 均采用 RBAC + ABAC；默认只读。
3. 分级自治：只读分析 → 低风险 Runbook → PR/灰度 → 有限无人值守。
4. 人机共治：高风险动作必须审批，AI 不直接覆盖生产脚本。
5. 证据链：输入、检索上下文、SQL、执行结果、变更、审批和验证结果可回放。
6. AI 嵌入对象：AI 出现在指标、资产、事件和任务页面中，不额外制造孤立聊天入口。
7. 确定性优先：指标编译、权限、拓扑、规则和验证优先于生成式推断。

## 2. 用户与场景

| 角色 | 主要任务 | 首要模块 |
|---|---|---|
| 经营/业务人员 | 问指标、看趋势、解释异常 | 智能问数、决策驾驶舱 |
| 数据分析师/数据开发 | 指标建模、SQL 复用、数据质量 | 问数、关系治理、质量中心 |
| DBA/数据架构师 | 表结构、血缘、慢 SQL、风险变更 | 关系治理、AIOps |
| SRE/运维 | 告警诊断、重跑、Runbook、事故复盘 | AIOps、指挥中心 |
| 管理者/值班长 | 经营与稳定性决策、审批 | 决策驾驶舱、审批中心 |
| 安全/审计 | 权限、敏感数据、操作审计 | 管理中心 |

首期优先场景：夜间跑批值守、数据库健康巡检、企业智能问数。第二阶段扩展发布风险评审、数据质量异常调查和生产故障作战室。根据《多场景探索》新增统一“场景中心”，首期纳入 12 个 Agent Team 场景模板，统一管理触发器、Agent 阵容、工具连接、运行任务、证据、审批与验收指标。

## 3. 信息架构与导航

```text
Aegis
├── 工作台（待办、告警、指标、最近会话）
├── 智能问数（自然语言分析、数据集、BI 结果、指标口径）
├── 知识库（问知识库、文档管理、检索测试、配置）
├── 数据资产（目录、表结构、字段 Comment、血缘与质量）
├── AIOps（事件中心、拓扑、根因分析、Runbook、事故复盘）
├── SQL 优化（版本化计划、真实 EXPLAIN、改写与索引建议）
├── 场景中心（模板、运行实例、Agent Team、审批、审计）
├── 任务与审批（任务队列、审批流、变更记录）
└── 管理中心（数据源、模型、知识库、权限、审计、系统设置）
```

全局能力：搜索、AI 助手、通知、快捷创建任务、租户/项目切换、环境切换（开发/测试/生产）。

## 4. 核心模块设计

### 4.1 智能问数

**用户流程**：输入问题 → 意图识别 → 匹配指标/语义模型 → 生成只读 SQL → 权限检查与 SQL 静态检查 → 执行 → 结果复核 → 图表/结论 → 追问、收藏或转任务。

**页面原型（桌面端）**

```text
┌─ 顶栏：Aegis | 工作区 | 全局搜索 | 通知 | 用户 ───────────────┐
├─ 侧栏：问数首页 / 指标目录 / 我的报表                           │
│  ┌─ 会话列表 ─────┐ ┌─ 对话与结果区 ─────────────┐ ┌─ 证据栏 ─┐ │
│  │ 最近会话        │ │ 问：本月华东收入同比？      │ │ 指标口径 │ │
│  │ + 新建会话      │ │ [SQL] [表格] [趋势图]       │ │ 数据源   │ │
│  │                 │ │ 结论：+12.4% 置信度 92%   │ │ 权限检查 │ │
│  └────────────────┘ │ 追问…  [导出] [转任务]      │ │ 审计ID   │ │
│                     └────────────────────────────┘ └──────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

关键交互：SQL/结果/图表 Tab；显示“为什么这样算”；一键纠正口径；危险字段自动脱敏；结果异常时要求用户确认时间范围或维度。

### 4.2 AIOps

事件进入后按“采集 → 检测 → 关联 → 根因 → 建议 → 审批 → 执行 → 验证 → 复盘”闭环。支持 Prometheus、Loki/OpenSearch、OpenTelemetry、Airflow/DolphinScheduler、Kubernetes、Git 等连接器。

**事件详情原型**：左侧事件时间线与影响范围；中间指标/日志/调用链证据；右侧根因假设、推荐 Runbook、风险等级、审批按钮；底部显示执行日志和验证结果。

自动化等级：L0 只读建议；L1 自动重试；L2 执行已审核 Runbook；L3 生成 Git PR；L4 生产变更（默认关闭，需双人审批和可回滚策略）。

### 4.3 数据关系展示与治理

对象模型：数据源、库、Schema、表、字段、指标、SQL、任务、服务、告警、负责人。

页面采用“目录树 + 关系画布 + 属性抽屉”：支持表结构查看、上下游血缘、字段级影响分析、SQL 搜索与归档、重复/高风险 SQL 识别、指标口径关联、标签和责任人维护。

血缘来源优先级：解析 SQL/ETL → OpenLineage → 调度依赖 → 人工确认。每条关系记录来源、更新时间和可信度。

### 4.4 智能自主指挥/决策

由“指挥官 Agent”协调专业 Agent Team：incident-lead、observability、database-dba、platform-sre、business-analyst、data-validator。一个事件对应一个根任务，子任务并行执行，证据和结论集中归档。

决策卡片字段：问题、影响、证据、候选方案、预期收益、风险、回滚、推荐方案、审批人、截止时间。支持模拟执行（dry-run）、人审、灰度、自动验证和升级人工。

### 4.5 其他场景工作台

以模板方式扩展：发布前风险评审、发布后观察窗、数据质量异常调查、客户工单诊断、安全漏洞响应、云成本优化、项目数字参谋部。每个模板包含触发器、Agent 角色、工具权限、Runbook、审批策略和验收规则。

### 4.6 场景中心（多场景协作控制平面）

场景中心将《多场景探索》的 12 个场景统一建模为“场景模板 → 运行实例 → Agent 步骤 → 工具动作 → 证据 → 审批 → 验证”的闭环。用户从场景模板库选择场景，填写本次目标和上下文，启动根任务；系统按步骤展示 Agent 状态和证据，高风险动作进入审批门禁，批准只对当前步骤生效。

12 个模板包括：夜间跑批值守、生产故障虚拟作战室、脚本异常自动修复、数据库每日健康巡检、发布前风险评审、发布后观察窗、数据质量异常调查、企业智能问数交付、客户工单诊断、安全漏洞响应、云成本优化和项目数字参谋部。

产品边界：本地 MVP 只记录确定性的演示执行证据，不虚构已经调用生产 Prometheus、Airflow、GitLab 或 Rundeck；真实动作通过 Adapter、Runbook 白名单、最小权限、审批和回滚策略接入。

### 4.7 企业知识库

知识库按“选择知识库 → 添加资料 → 检索问答 → 核验引用”设计单页闭环。资料支持文本、文件、本机允许目录以及后续 Wiki/Git/网盘连接器。回答必须返回文档、片段、相关度和来源；没有足够证据时明确拒答。知识库通过 Retriever Tool 供智能问数、AIOps、SQL 优化与场景中心复用，详细设计见《知识库模块产品与研发设计方案》。

## 5. 前端页面与交互规范

技术建议：React + TypeScript + Ant Design/Arco Design；数据图表 ECharts；关系图 React Flow/Cytoscape；代码与 SQL Monaco Editor；实时事件 WebSocket/SSE。

视觉方向：企业控制台风格，浅色为默认、深色可切换；8px 圆角、信息密度优先；状态色统一为蓝（进行中）、绿（成功）、橙（待审批）、红（风险）、灰（已归档）。

通用组件：AppShell、CommandPalette、EvidencePanel、MetricCard、SQLViewer、LineageCanvas、IncidentTimeline、ApprovalDrawer、RunbookStep、AgentRoster、AuditTrail。

响应式策略：桌面端三栏；平板端折叠证据栏；移动端只提供告警、审批、任务跟进，不承载复杂血缘编辑和 SQL 编写。

## 6. 技术架构规划

```text
浏览器
  ↓ HTTPS / SSE / WebSocket
BFF/API Gateway ── IAM(SSO/OIDC、RBAC/ABAC、租户隔离)
  ├─ Query Copilot（意图、语义层、SQL 生成、验证、图表）
  ├─ AIOps Engine（事件、规则/异常检测、RCA、策略）
  ├─ Agent Orchestrator（LangGraph/Temporal；任务、记忆、工具调用）
  ├─ Governance Service（目录、血缘、SQL、质量、元数据）
  ├─ Workflow/Approval（审批、Runbook、灰度、回滚）
  └─ Audit/Notification（审计、飞书/企微/钉钉）
        ↓
数据与执行底座：PostgreSQL/TiDB、Redis、OpenSearch、对象存储、向量库
连接器：MySQL/TiDB/PG/数仓、Airflow/DolphinScheduler、Prometheus/Loki、K8s、Git、工单系统
```

推荐后端：Go/Java 微服务或模块化单体起步；Python 用于模型与数据分析。工作流用 Temporal；策略与 Runbook 用 OPA + Rundeck/StackStorm；密钥用 Vault/OpenBao；部署支持 Docker Compose（PoC）和 Kubernetes（生产）。

模型网关统一接入企业私有模型和云模型，支持模型路由、Token 预算、敏感信息脱敏、Prompt 版本、评测集和降级策略。

## 7. 数据模型与安全

核心实体：Tenant、User、DataSource、Asset、Metric、SqlQuery、LineageEdge、Incident、Agent、Runbook、Approval、Execution、Evidence、AuditEvent。

安全控制：列/行级权限、动态脱敏、SQL AST 白名单、只读连接、查询超时/扫描量上限、生产环境网络隔离、命令白名单、双人审批、短期凭证、全量审计。所有 AI 输出标注生成时间、模型版本、证据引用和可验证状态。

## 8. MVP 范围与验收

**MVP（8～12 周）**：SSO/RBAC；接入 2 种数据库、Prometheus、日志系统和 1 个调度平台；问数支持 20 个核心指标；事件中心 + RCA 摘要；10 个只读/低风险 Runbook；数据目录/表结构/基础血缘；任务、审批、审计；飞书通知。

验收指标：问数有效回答率 ≥80%；SQL 执行拦截 100% 越权与写操作；P1 事件 RCA 首次建议 ≤5 分钟；低风险 Runbook 成功率 ≥95%；审计事件完整率 100%；核心资产血缘覆盖率 ≥70%。

## 9. 路线图

| 阶段 | 时间 | 重点 |
|---|---|---|
| P0 验证 | 0-4 周 | 单租户、只读问数、事件汇总、数据目录原型 |
| P1 MVP | 5-12 周 | 三大核心模块、审批审计、低风险 Runbook |
| P2 扩展 | 3-6 月 | Agent Team、PR 修复、质量中心、作战室、更多连接器 |
| P3 自治 | 6-12 月 | 策略学习、灰度发布、有限无人值守、行业模板市场 |

## 10. 运营与评测

建立离线评测集（问题-SQL-结果-口径）、线上反馈（正确/错误/纠正）、安全红队集（越权、注入、破坏性 SQL）、AIOps 回放集（历史告警与真实处置）。每周评审回答质量、误报漏报、自动化成功率、人工接管率和成本。

## 11. 风险与决策门槛

| 风险 | 控制措施 |
|---|---|
| AI 生成错误 SQL/结论 | 语义层、只读、AST 检查、结果复核、引用证据 |
| 自动操作扩大事故 | 分级自治、审批、命令白名单、dry-run、回滚 |
| 数据泄露 | 租户隔离、行列权限、脱敏、私有化模型、审计 |
| 供应商锁定 | 连接器 SPI、模型网关、标准 OpenTelemetry/OpenLineage |
| Agent 失控 | 工具沙箱、预算/超时、最大步数、人工接管 |

**上线门槛**：没有负责人、权限、回滚和验证规则的动作不得进入自动执行；涉及删表、资金、订单、生产配置和破坏性 DDL 的动作永久要求人工审批。

## 12. 下一步产品工作

1. 选定一个真实业务域，盘点 20 个核心指标、10 张关键表和 5 类高频告警。
2. 形成数据源、权限、指标、Runbook、审批人清单，完成 PoC 接入。
3. 用历史问题构建问数与 AIOps 评测集，定义上线前质量门槛。
4. 以“夜间跑批值守 + 智能问数 + 数据关系目录”做首个试点，四周一次复盘。

## 13. 登录后的产品使用路径

产品不把用户直接丢进空白聊天框。用户登录后先完成工作区、身份和待办确认，再按任务入口进入功能页面。

### 13.1 首次登录

```text
登录/SSO → 选择工作区 → 检查默认模型
→ 无模型：模型接入/规则模式 → 工作台 → 推荐的首个任务
→ 已有模型：直接进入工作台
```

首次登录先检查工作空间是否已有可用默认模型。若没有，用户进入“模型接入”引导，选择公有 API 或私有部署，填写服务地址、模型 ID 和凭证，连接测试通过后进入工作台；也可明确选择“规则模式”稍后配置。共享生产工作空间的模型通常由管理员预先配置，普通用户直接复用，不重复索取密钥。

模型接入后的页面路径为：`登录 → 模型接入 → 公有/私有 → Provider → 连接参数 → 保存并测试 → 设为默认 → 工作台`。连接失败时保留错误状态和重试入口，不允许未验证连接成为默认模型；API Key 不回显。

### 13.2 常规登录

```text
登录 → 工作台
├─ 待我处理：审批、失败事件、待确认问数
├─ 关注指标：经营指标和稳定性指标
├─ 最近工作：问数、Incident、资产
└─ 快捷入口：问一个数 / 查一个资产 / 处理一个事件
```

工作台只保留用户当前需要处理的内容，空状态提供明确的下一步按钮，例如“开始第一个问数”“配置数据源”“查看演示指标”。

## 14. 页面级产品流程

### 14.1 工作台

进入条件：登录成功且工作区已确定。主操作：点击待办、快捷入口或最近工作。成功结果：进入具体任务页面并保留返回上下文。下一步：完成任务后回到工作台，卡片状态更新。

页面固定区域：顶部工作区/环境；左侧一级导航；中部待办和指标；右侧通知。生产环境固定显示“生产”标签和当前用户权限。

### 14.2 智能问数

进入：点击“问一个数”或导航“智能问数”。

主流程：选择问题模板或直接输入 → AI 展示理解结果 → 用户补充时间/维度 → 查看结论、图表和证据 → 追问/导出/收藏/转任务。

页面必须始终有一个明确的主按钮：无结果时为“开始分析”，需要澄清时为“确认并继续”，有结果时为“转为报表”或“转为任务”。

异常路径：无权限显示缺少的指标/数据源；口径不明确显示候选定义；SQL 被拦截显示原因和修改建议；结果异常显示“需要人工确认”，不输出确定性结论。

### 14.3 AIOps 事件

进入：工作台 P0/P1 卡片或“事件中心”。

主流程：筛选事件 → 打开事件详情 → 查看影响和证据 → 接受 RCA 建议或继续调查 → 选择 Runbook → dry-run → 审批/执行 → 验证 → 关闭并生成复盘。

页面主操作随状态变化：开放事件显示“开始分析”；已诊断显示“查看处置方案”；待审批显示“处理审批”；执行中显示“查看执行”；已恢复显示“完成复盘”。

### 14.4 数据治理

进入：导航“数据治理”或问数结果中的资产链接。

主流程：搜索资产 → 查看 Schema → 展开上下游血缘 → 查看关联 SQL/指标 → 标记负责人和敏感等级 → 提交治理任务。

治理页面不要求用户先理解图谱。默认先展示资产摘要和“影响范围”，用户点击“展开血缘”后再加载复杂关系。

### 14.5 智能指挥/作战室

进入：从 Incident、任务或“新建作战室”进入。页面先展示问题、影响、时间线和当前决策，不先展示 Agent 技术细节。

主流程：确认目标 → 查看 Agent 分工 → 查看证据 → 选择候选方案 → 模拟执行 → 审批 → 执行/灰度 → 验证 → 形成结论。

### 14.6 任务与审批

任务页回答“我需要做什么、为什么、什么时候完成”；审批页回答“批准什么、风险是什么、失败如何回滚”。两个页面通过任务 ID、Incident ID 和执行 ID互相跳转，避免用户在多个列表间寻找上下文。

## 15. 导航和信息层级优化

一级导航按用户任务固定为：工作台、智能问数、知识库、数据资产、TiDB 结构、AIOps 事件、SQL 优化、场景中心、模型接入。工作台提供经营数据分析、查询企业知识、诊断 SQL 性能和发起协同任务四个快捷入口；模型接入位于平台管理分组，系统设置保留在侧栏底部。

产品中将“问数、事件、资产、任务”作为四类核心对象，所有详情页使用统一的对象头、状态、负责人、时间、关联对象和审计入口。用户可从任一对象跳到相关对象并返回原位置。

全局搜索支持“指标/表/字段/SQL/事件/任务/Runbook”分类结果；结果卡片必须显示对象类型、状态、负责人和最近更新时间。

## 16. 产品体验验收

1. 新用户登录后 10 秒内能找到“经营数据分析”“查询企业知识”“诊断 SQL 性能”或“发起协同任务”。
2. 用户无需记住系统术语即可完成问数、查看事件和查资产三条主流程。
3. 每个页面只有一个主操作，危险操作与普通查看操作视觉和权限明显区分。
4. 任意异步操作都有进度、当前步骤、预计等待和失败后的下一步。
5. 从结果到资产、任务、审批、复盘可通过页面按钮完成，不依赖返回列表重新搜索。
6. 用户在任何详情页都能看到当前环境、负责人、状态和审计入口。

竞品与开源项目的详细学习结论见[竞品与开源项目学习报告](企业AI落地平台-竞品与开源项目学习报告.md)。产品采用 WrenAI/SuperSonic 的语义层思路、OpenMetadata/DataHub 的资产对象模式、HolmesGPT 的证据化诊断模式和 Rundeck/StackStorm 的受控执行模式，但统一交互、权限、任务、审批和审计由 Aegis 自身控制面负责。

## 附录 A：参考资料与本方案假设

本方案吸收了以下参考文档的结论：

- [智能问数场景](https://pingcap-cn.feishu.cn/wiki/JJpUwKZ4FiuI6lkCkCQcHf3bn8f)：企业问数的关键是语义层、指标口径、权限、SQL 安全、结果验证和审计。
- [AIOPS](https://pingcap-cn.feishu.cn/wiki/KiZCwMR9ziz47UkgXbtcekrdnvg)：采用“采集—异常检测—根因分析—Runbook—验证”的闭环，生产脚本通过 Git 分支、测试和审批发布。
- [多场景探索](https://pingcap-cn.feishu.cn/wiki/SRmBwpk7QiaXZFkM9bYciwWfnJe)：以 AI 协作与控制平面承接 Agent Team、定时触发、机器执行、任务留痕和人工介入。

假设：首期部署在企业内网或专有云；已有统一身份认证；允许通过 API/CLI 接入数据库、调度、监控和代码平台；模型可采用企业私有模型或经审批的外部模型。具体行业指标、数据分级、审批矩阵和 SLA 需在试点阶段补齐。

## 17. v2 模块与功能落地契约

为避免“产品方案有模块、代码只有页面、用户不知道下一步”的断层，产品功能统一登记在《模块与功能详细设计方案》和 `backend/app/product_catalog.py`。当前 8 个一级模块、63 项功能都具备功能 ID、角色、入口、输入、输出、门禁和交付状态。

| 一级模块 | 子功能边界 | 当前可操作入口 | 生产前必须补齐 |
|---|---|---|---|
| 智能问数 | 数据源管理、ChatBI、认可报表大屏、MCP 元数据、语义匹配、Text2SQL、复核、BI、文件/目录分析 | 智能问数、数据资产、TiDB 结构 | 指标注册、ACL、生产凭证库、模型网关 |
| 企业知识库 | 空间、入库、Chunk、检索测试、引用问答、反馈、连接器 | 知识库四个任务视图 | PostgreSQL/MinIO、向量索引、文档 ACL、异步索引 |
| 数据资产与治理 | 目录、Schema、血缘、影响分析、SQL 资产、质量、责任人 | 数据资产、TiDB 结构、场景中心 | OpenMetadata/DataHub、OpenLineage、质量平台 |
| AIOps 事件与巡检 | 事件、证据、RCA、Runbook、跑批、数据库巡检、观察窗、复盘 | AIOps 事件、场景中心、SQL 优化 | Prometheus/Loki/Tempo、调度、执行网关 |
| TiDB SQL 优化 | 多输入、版本画像、模拟计划、真实 EXPLAIN、建议、DDL 评审 | SQL 优化、场景中心 | 版本匹配的 TiDB 集群、只读账号、变更审批 |
| 场景中心与智能指挥 | 12 个场景模板、Agent Team、决策、证据和验证 | 场景中心 | Temporal、Connector SPI、真实工具 Adapter |
| 任务/审批/执行 | 队列、风险、审批、执行、验证、回滚 | 场景运行面板 | IAM、OPA、Rundeck/StackStorm、短期凭证 |
| 平台管理与集成 | 数据源、模型、连接器、IAM、策略、审计、健康 | 功能中心和系统设置 | SSO、Vault/OpenBao、审计存储、配置中心 |

功能中心按角色筛选并显示“可使用 / 演示闭环 / 待生产接入”，可使用和演示功能直接跳转到页面；规划功能不提供虚假入口。详细输入输出、门禁与 12 个场景映射见[模块与功能详细设计方案](企业AI落地平台-模块与功能详细设计方案.md)。

## 18. 业务主流程验收

1. 业务用户：工作台 → 经营数据分析 → 问题 → 口径/SQL/结果 → 图表与证据。
2. DBA：功能中心选择 DBA → TiDB 结构 → SQL 优化 → 版本计划 → 建议 → 测试/变更评审。
3. SRE：工作台待关注 → Incident → 证据/RCA → 场景作战室 → 审批 → 验证/复盘。
4. 数据工程师：功能中心选择数据角色 → 资产/血缘 → 质量调查 → 修复任务 → 复验。
5. 管理员：功能中心选择管理员 → 连接/模型/策略能力 → 配置向导 → 健康检查 → 审计。

每条流程都必须有唯一主操作、明确完成态、错误重试和下一步；用户不需要手工拼接 ID 或跳转外部系统才能完成本地 MVP 主链路。
