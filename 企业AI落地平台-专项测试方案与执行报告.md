# 企业 AI 落地平台专项测试方案与执行报告

版本：v1.0
日期：2026-08-19
测试对象：Data AI Explorer / Aegis AI Control Plane MVP
测试结论：当前 MVP 可完成演示级前端主流程；尚未达到产品设计中“真实业务可用、生产可部署”的上线标准。

## 1. 测试目标

验证系统是否能够按照产品设计完成以下闭环：

1. 用户登录后进入工作台，能够找到待处理事项和核心入口。
2. 用户可以发起智能问数，查看结果、证据和下一步操作。
3. 用户可以查看 AIOps 事件并进入诊断、处置、验证流程。
4. 用户可以搜索数据资产，查看详情、Schema 和关系信息。
5. 前端、后端、部署配置能在本地环境启动并完成基础联调。
6. 系统具备面向权限、安全、可靠性、性能和审计的质量门槛。

## 2. 当前版本范围

### 已实现范围

- React/Vite 前端：登录、工作台、智能问数、AIOps 事件列表、数据资产目录、退出登录。
- FastAPI 演示后端：健康检查、工作台摘要、问数提交/状态、事件列表/详情、资产列表/详情。
- Docker Compose：Web、FastAPI、PostgreSQL、Redis、MinIO。
- 本地演示数据和模拟问数流式答案。

### 未实现或仅有占位

- 企业 SSO/OIDC、真实用户、RBAC/ABAC、租户隔离。
- 前端对 FastAPI API 的真实调用和错误降级。
- 真实数据库查询、语义层、SQL AST 安全检查、模型网关。
- AIOps 事件详情、RCA、证据采集、Runbook、审批、Executor。
- 数据资产详情、Schema、血缘画布、治理任务。
- PostgreSQL/Redis/MinIO 的业务持久化和迁移。
- Kafka、Temporal、OpenTelemetry、真实模型服务。

## 3. 测试策略

测试采用五层金字塔：

```text
静态检查/构建
  ↓
单元与领域规则
  ↓
API 契约与接口集成
  ↓
浏览器 E2E 与视觉回归
  ↓
部署、性能、安全、容灾和验收测试
```

测试原则：关键路径优先、失败可复现、数据可重置、权限拒绝必须可验证、异步流程以服务端最终状态为准、任何危险动作都必须验证审计和回滚。

## 4. 测试环境

### 4.1 开发冒烟环境

- macOS/Linux
- Node.js 22+
- Python 3.12+
- Docker Desktop + Compose v2
- Chromium/Codex in-app Browser

### 4.2 目标集成环境

- Kubernetes 1.28+
- PostgreSQL 16、Redis 7、OpenSearch、Kafka、Temporal、MinIO/S3
- Prometheus、Loki/OpenSearch、Alertmanager
- OIDC/SAML、Vault/OpenBao
- 一个 OpenAI-compatible 自建模型和一个本地 Embedding 模型

## 5. 测试数据

### 5.1 用户与权限

| 用户 | 角色 | 预期 |
|---|---|---|
| analyst-a | Analyst | 只能查询授权指标和只读资产 |
| operator-a | Operator | 可查看事件、执行 Low Runbook |
| approver-a | Approver | 可处理审批，不可修改策略 |
| auditor-a | Auditor | 只能查看审计，不可执行动作 |
| tenant-b-user | 其他租户 | 不可看到 tenant-a 任何对象 |

### 5.2 业务与运维数据

- 20 个已发布指标：GMV、收入、订单量、取消率、留存率等。
- 10 张表：订单、客户、商品、支付、日报等，包含字段密级和负责人。
- 5 类事件：连接超时、资源不足、数据未到、Schema 变更、代码错误。
- 10 个 Runbook：重试、Pod 重启、扩容、任务补跑、数据校验和回滚。
- 越权 SQL、DDL/DML、多语句、危险函数、Prompt Injection 和敏感日志样本。

## 6. 功能测试用例

### 6.1 登录与工作台

| ID | 场景 | 操作 | 预期 |
|---|---|---|---|
| AUTH-001 | 正常登录 | 输入合法账号并提交 | 进入工作台，显示工作区/环境 |
| AUTH-002 | 空账号 | 不填写账号提交 | 阻止提交并显示字段错误 |
| AUTH-003 | 错误密码 | 输入错误密码 | 后端拒绝，保留可理解错误 |
| AUTH-004 | 会话过期 | 访问受保护页面 | 跳转登录，不泄露页面数据 |
| AUTH-005 | 退出登录 | 点击退出 | 清理会话并回到登录页 |
| WB-001 | 工作台待办 | 点击事件/任务卡片 | 进入对应详情，返回后状态刷新 |
| WB-002 | 工作台空态 | 无事件、无问数、无资产 | 显示明确下一步，不显示空白模块 |

### 6.2 智能问数

| ID | 场景 | 操作 | 预期 |
|---|---|---|---|
| QUERY-001 | 简单问题 | 输入“近30天 GMV 趋势” | 返回 202 和 operation_id，前端展示进度 |
| QUERY-002 | 结果展示 | 等待完成 | 显示结论、图表、数据、SQL、证据和审计 ID |
| QUERY-003 | 需要澄清 | 输入无时间范围问题 | 显示时间/指标/维度选项，不执行查询 |
| QUERY-004 | 越权查询 | 查询无权限字段 | 拒绝执行，显示策略原因 |
| QUERY-005 | 写 SQL | 输入 DROP/UPDATE/DELETE | 100% 阻断并审计 |
| QUERY-006 | 查询超时 | 模拟数据源超时 | 显示可重试/转人工，不重复执行副作用 |
| QUERY-007 | SSE 断线 | 中途断网后恢复 | 使用 Last-Event-ID 或 GET 最终状态恢复 |
| QUERY-008 | 导出/转任务 | 点击结果下一步 | 创建导出或任务并带入问题、SQL、证据 |

### 6.3 AIOps

| ID | 场景 | 操作 | 预期 |
|---|---|---|---|
| OPS-001 | 事件列表 | 按等级/状态/服务筛选 | URL 可复现，结果准确 |
| OPS-002 | 事件详情 | 点击 P1 事件 | 显示影响、时间线、证据、RCA 和 Runbook |
| OPS-003 | RCA | 发起诊断 | 规则/拓扑/指标证据先于 LLM 结论 |
| OPS-004 | Low Runbook | dry-run 后执行 | 执行、验证、通知和审计完整 |
| OPS-005 | High Runbook | 申请执行 | 必须双人审批和灰度 |
| OPS-006 | Critical 动作 | 尝试执行删表/生产配置 | 永久禁止无人值守 |
| OPS-007 | 执行不确定 | Executor 断线 | 标记 HUMAN_REQUIRED，禁止盲目重试 |

### 6.4 数据治理

| ID | 场景 | 操作 | 预期 |
|---|---|---|---|
| GOV-001 | 资产搜索 | 搜索表/字段/指标 | 返回类型、负责人、密级、更新时间 |
| GOV-002 | 资产详情 | 点击资产 | 显示概要、Schema、质量、SQL、治理记录 |
| GOV-003 | 血缘 | 展开上下游 | 默认一跳，按需加载，超过阈值提示缩小范围 |
| GOV-004 | 权限 | 访问受限字段 | 脱敏或拒绝，不泄露 Schema |
| GOV-005 | 治理任务 | 提交负责人/密级变更 | 生成任务并可追踪审批 |

## 7. 接口测试

### 7.1 正向接口

验证 `/health`、工作台摘要、问数提交/查询、事件列表/详情、资产列表/详情的 HTTP 状态、JSON Schema、字段类型、错误码和 CORS。

### 7.2 负向接口

- 缺少必填字段、空字符串、超长问题、非法枚举。
- 不存在的 operation、incident、asset 返回 404。
- 查询筛选组合、分页边界、重复 Idempotency-Key。
- 未认证、无权限、跨租户 resource ID。
- 后端依赖不可用、超时、重复消息和乱序事件。

### 7.3 API 质量门槛

- 4xx/5xx 遵循统一错误结构，不返回堆栈和凭证。
- 所有写接口具备幂等策略。
- 异步接口 202 后状态可查询，失败状态可重试或取消。
- OpenAPI 与实际响应无漂移。

## 8. 安全测试

| 类别 | 测试内容 |
|---|---|
| 认证 | OIDC 回调、Token 过期、重放、登出、MFA |
| 授权 | RBAC、ABAC、行列权限、环境策略、跨租户串读 |
| SQL | DDL/DML、多语句、注释绕过、危险函数、扫描量 |
| AI | Prompt Injection、工具参数注入、越权上下文、模型降级越界 |
| 执行 | 任意 Shell、插件越权、签名任务伪造、重放、回滚绕过 |
| Web | XSS、CSRF、CSP、依赖漏洞、CSV Formula Injection |
| 数据 | 日志脱敏、对象存储 URL 过期、Secret 泄露、备份权限 |

安全阻断项：任意命令执行、跨租户数据、无审批生产动作、明文 Secret、模型将受限数据发送到公网 Provider。

## 9. 性能与稳定性测试

### 9.1 目标

| 场景 | 目标 |
|---|---|
| 普通 API | P95 <500ms |
| 资产搜索 | P95 <1s |
| 两跳血缘 | P95 <2s |
| 问数首个状态 | <2s |
| 简单问数完整结果 | P95 <15s |
| 告警接收 | 峰值 1,000 event/s，P95 <5s 入队 |
| P1 初次诊断 | ≤5 分钟 |

### 9.2 场景

- 100 并发用户同时打开工作台。
- 50 并发问数，混合简单/澄清/越权/超时问题。
- 1,000 event/s 告警峰值，验证 Kafka lag 和事件聚合。
- 500 节点和 5,000 节点血缘图查询。
- 模型延迟、Provider 限流、GPU 显存 85% 和连接器故障。
- 连续 24 小时稳定性、内存泄漏和队列积压恢复。

## 10. 部署与容灾测试

- `docker compose config --quiet` 配置校验。
- 本地冷启动、重复启动、停止、重置卷、端口冲突。
- Backend/Web/DB/Redis/MinIO 单组件重启。
- Kubernetes Pod 驱逐、节点故障、滚动升级、Canary、回滚。
- PostgreSQL 全量/WAL 恢复，MinIO 对象恢复，OpenSearch 重建。
- 完全离线环境导入镜像、模型和 Helm，禁用公网后核心功能可用。
- Executor 状态未知恢复，暂停自动执行并人工核对。

验收：RPO ≤15 分钟、RTO ≤60 分钟；恢复后审计、审批、Workflow 和执行状态不丢失。

## 11. 可用性与兼容性测试

- 1280px、1440px 桌面宽度：无重叠、截断、横向滚动异常。
- 1024px 平板：证据栏可折叠，主操作仍可见。
- 移动端：事件查看、任务跟进、审批可完成。
- 键盘导航、焦点可见、屏幕阅读器标签、状态不依赖颜色。
- Chrome/Edge 最近两个版本。
- 网络慢、断网、SSE 断线、后台切换、浏览器刷新。

## 12. 自动化测试建设

### 前端

- Vitest：状态机、格式化、图表适配、权限显示。
- Testing Library：登录、问数进度、证据、审批、错误状态。
- Playwright：登录 → 工作台 → 问数 → 事件 → 资产 → 退出主流程。
- 视觉回归：登录、工作台、问数结果、事件详情、资产详情。

### 后端

- Pytest：Pydantic 校验、状态机、策略、脱敏、幂等。
- TestClient/HTTPX：接口正负向和 RFC 9457 错误。
- Testcontainers：PostgreSQL、Redis、Kafka、OpenSearch、Temporal。
- 历史事件回放和问数评测集。

### CI 门禁

```text
格式/类型检查 → 单元测试 → API 契约 → 安全扫描
→ 前端构建 → E2E → 性能烟测 → 生成报告
```

## 13. 本次执行记录（MVP）

执行时间：2026-08-19。测试环境：本地 Vite 开发服务器、Codex in-app Browser、Node.js 23、Python 3.9 环境。

| 检查项 | 结果 | 证据/说明 |
|---|---|---|
| 前端依赖安装 | 通过 | `npm install` 无漏洞 |
| 前端 TypeScript + Vite 构建 | 通过 | `npm run build` 生成 dist |
| 后端 Python 编译 | 通过 | `python3 -m compileall -q backend` |
| Compose 配置 | 通过 | `docker compose config --quiet` |
| 登录页渲染 | 通过 | 页面可见账号、密码、登录按钮 |
| 登录进入工作台 | 通过 | 浏览器操作验证 |
| 工作台内容 | 通过 | 指标、事件、最近问数可见 |
| 智能问数输入 | 通过 | 输入问题并开始分析 |
| 模拟流式结果 | 通过 | 结果、证据、图表容器出现 |
| AIOps 事件列表 | 通过 | P1/P2/P3 事件和状态可见 |
| 数据资产目录 | 通过 | 资产卡片、质量、负责人、行数可见 |
| 退出登录 | 通过 | 返回登录页 |
| FastAPI 运行时接口 | 未完成 | 当前 Python 解释器未预装 FastAPI；依赖安装/运行测试环境需单独执行 |
| Docker 完整启动 | 未完成 | 当前执行环境未确认 Docker Engine 可用 |
| 前后端真实联调 | 未通过 | 前端当前使用演示数据和本地模拟流式回答 |

## 14. 当前缺陷清单

| 编号 | 严重度 | 缺陷 | 影响 | 修复建议 |
|---|---|---|---|---|
| BUG-001 | P0 | 登录仅切换前端状态，不校验账号/密码 | 任何人可进入系统，不能上线生产 | 接入 OIDC/SAML，服务端会话和过期处理 |
| BUG-002 | P1 | 前端未调用 FastAPI，工作台/事件/资产为硬编码 | 页面数据不会随后端变化，无法真实联调 | 引入 API SDK、Query Cache、加载/错误/空态 |
| BUG-003 | P1 | 问数结果为前端模拟答案 | 不能验证 SQL、安全、真实模型和审计 | 调用 202 operation API 和状态流，接入 Model Gateway |
| BUG-004 | P1 | AIOps 事件行无详情跳转，RCA/Runbook 未实现 | 无法完成事件处置闭环 | 增加事件详情路由、证据、审批、执行和验证 |
| BUG-005 | P1 | 数据资产搜索、详情按钮未形成真实操作 | 无法治理 Schema、血缘和责任人 | 接入 assets API、详情路由和血缘接口 |
| BUG-006 | P1 | 后端使用内存数据，重启即丢失 | 不具备持久化和审计能力 | PostgreSQL migration、Repository、RLS |
| BUG-007 | P1 | 后端问数接口返回 202 但立即 completed | 不符合异步工作流契约 | Temporal/队列持久化 operation 状态，支持取消/重试 |
| BUG-008 | P1 | Compose 后端启动时每次执行 pip install | 启动慢且依赖外部网络，不适合离线 | 构建锁定 Docker 镜像，依赖预装并做离线包 |
| BUG-009 | P2 | 没有自动化测试和 CI 门禁 | 回归风险高 | 按本方案建立 Vitest/Pytest/Playwright 流水线 |
| BUG-010 | P2 | MinIO healthcheck 依赖镜像内 `mc` 可执行文件 | 可能造成健康检查误报 | 改为 HTTP health endpoint 或专用 mc 镜像 |

## 15. 上线判定

当前状态：**不通过生产上线，仅通过演示验收**。

必须修复 BUG-001～BUG-008，并完成以下门槛后才可进入试点：

- 前后端真实联调，核心页面数据来自 API。
- OIDC/RBAC/租户隔离和 SQL 写操作拦截通过安全测试。
- 问数、Incident、资产三条链路可点击完成并可审计。
- PostgreSQL 持久化、备份恢复、操作幂等和错误降级通过。
- 至少一套 Playwright E2E、Pytest API、权限和 SQL 安全集合进入 CI。
- Docker Compose 冷启动和离线安装通过。

## 16. 后续测试执行计划

1. 修复 P0/P1 缺陷后执行 API 契约和前后端联调回归。
2. 接入真实数据库/模型/监控后执行问数与 AIOps 评测集。
3. 增加权限矩阵、SQL 对抗、Prompt Injection 和任意命令安全测试。
4. 完成 100 并发、1,000 event/s、24 小时稳定性和恢复演练。
5. 生成测试报告、缺陷关闭证明和上线评审结论。
