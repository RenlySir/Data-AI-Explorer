# 企业 AI 落地平台原型设计方案

版本：v1.0
日期：2026-08-19
产品：Aegis AI Control Plane

本文是产品设计方案的页面级原型稿。它不追求视觉稿的像素精度，而是定义页面布局、点击入口、操作结果、页面跳转和异常状态，供 UI 设计、前端开发和产品验收共同使用。

## 1. 原型目标

用户登录后不需要学习复杂概念，按“工作台 → 任务详情 → 处理 → 验证 → 归档”的路径即可完成工作。每个页面遵循：一个页面目标、一个主操作、一个明确结果、一个下一步。

## 2. 全局布局

```text
┌──────────────────────────────────────────────────────────────────┐
│ Aegis  工作区▼  环境: 测试▼     全局搜索       通知  帮助  用户▼ │
├───────────────┬──────────────────────────────────────────────────┤
│ 工作台         │ 页面标题 / 面包屑 / 对象状态 / 负责人             │
│ 智能问数       │                                                  │
│ AIOps         │                 页面内容区                        │
│ 数据治理       │                                                  │
│ 智能指挥       │                                                  │
│ 任务审批       │                                                  │
│ 管理中心       │                                                  │
└───────────────┴──────────────────────────────────────────────────┘
```

规则：左侧只展示一级导航；当前环境持续可见；详情页顶部保留返回；页面底部操作栏只展示当前状态允许的动作。

## 3. 登录与首次引导

### 3.1 登录页

```text
┌─────────────────────────────┐
│ Aegis                       │
│ 企业 AI 数据与运维工作台      │
│                             │
│ [ 使用企业 SSO 登录 ]         │
│                             │
│ 服务地址 / 版本 / 帮助         │
└─────────────────────────────┘
```

点击 SSO 后跳转企业认证；认证失败保留错误原因和重新登录；不在登录页展示系统复杂功能说明。

### 3.2 首次登录向导

```text
步骤 1 工作区 → 步骤 2 默认环境 → 步骤 3 关注领域 → 完成
┌─────────────────────────────────────┐
│ 选择你要工作的空间                    │
│ ○ 数据分析工作区                      │
│ ○ 运维值守工作区                      │
│                 [上一步] [进入工作台] │
└─────────────────────────────────────┘
```

只收集必要信息。管理员配置缺失时显示“联系管理员/查看演示数据”，不让用户卡在空白页面。

## 4. 工作台原型

```text
┌─ 工作台 ───────────────────────────────────────────────┐
│ 早上好，张三     当前：数据分析工作区 / 测试环境           │
│ [问一个数] [查看事件] [查数据资产]                         │
├─ 待我处理 ───────────────────────────────────────────────┤
│ 审批 2    待确认问数 1    P1事件 1    失败任务 3            │
├─ 重点关注 ───────────────┬─ 最近工作 ────────────────────┤
│ 本月收入  ¥12.4M  ↑12.4% │ 华东收入同比       10分钟前     │
│ P1 跑批失败  待处理       │ customer_order表  昨天         │
└──────────────────────────┴──────────────────────────────┘
```

点击卡片直接进入对应详情；完成后返回工作台，数字和状态自动刷新。无数据时替换为三个空状态动作，不显示空白图表。

## 5. 智能问数原型

### 5.1 会话页

```text
┌─ 智能问数 ───────────────────────────────────────────────┐
│ 会话列表       │ 你想了解什么？                           │
│ + 新建会话     │ [本月华东收入同比？________________] [开始分析]│
│ 最近会话       │ 模板：收入趋势 | 跑批概览 | 数据质量        │
│ 收藏报表       │                                          │
└────────────────┴──────────────────────────────────────────┘
```

### 5.2 结果页

```text
┌─ 本月华东收入同比？ ─────────────────────────────────────┐
│ ← 返回  状态：已完成  [转为报表] [转为任务]                │
├──────────────┬──────────────────────────┬───────────────┤
│ 会话消息       │ 结论：收入同比 +12.4%     │ 证据           │
│                │ 置信度 92%               │ 指标：收入     │
│ 追问输入       │ [趋势图] [表格] [SQL]     │ 口径：已发布v3 │
│                │ [正确] [需修正]            │ 数据源/权限/审计│
└──────────────┴──────────────────────────┴───────────────┘
```

点击证据字段可展开来源；点击“需修正”选择口径、时间、维度或结果错误；点击“转为任务”进入任务创建抽屉并自动带入问题、SQL 和证据。

### 5.3 问数状态

```text
输入 → 正在理解 → 检查口径 → 执行查询 → 验证结果 → 完成
```

需要澄清时页面显示选项卡：`时间范围`、`指标口径`、`统计维度`，主按钮变为“确认并继续”。被拒绝时显示拒绝原因、风险规则和“修改问题”。

## 6. AIOps 事件原型

### 6.1 事件中心

```text
┌─ AIOps / 事件中心 ──────────────────────────────────────┐
│ 筛选：等级▼ 服务▼ 环境▼ 状态▼ 时间▼     [刷新]            │
├────┬───────────────┬────────┬───────┬────────┬───────────┤
│等级│事件             │影响     │状态    │负责人   │操作       │
│P1  │订单跑批失败      │华东     │待分析  │李四     │[开始分析]  │
│P2  │API错误率升高     │支付     │已恢复  │王五     │[看复盘]    │
└────┴───────────────┴────────┴───────┴────────┴───────────┘
```

### 6.2 事件详情

```text
┌─ P1 订单跑批失败 ───── 待分析 / 测试 / 李四 ─────────────┐
│ 影响：订单日报延迟 42 分钟      [开始分析] [转作战室]      │
├─ 时间线 ───────────┬─ 证据 ─────────────┬─ 处置方案 ─────┤
│ 01:02 任务失败       │ 日志：连接超时      │ 根因候选 92%   │
│ 01:03 自动聚合       │ 指标：连接数正常    │ 重试 Runbook   │
│ 01:05 RCA 完成       │ 变更：无            │ 风险：低        │
│                     │                    │ [dry-run]      │
└─────────────────────┴────────────────────┴────────────────┘
```

dry-run 成功后主按钮变为“申请执行”；审批后变为“执行 Runbook”；执行结束后变为“查看验证”。失败时显示“转人工”和“查看日志”。

## 7. 数据治理原型

### 7.1 数据目录

```text
┌─ 数据治理 / 数据目录 ───────────────────────────────────┐
│ 搜索表、字段、指标、SQL    类型▼ 密级▼ 负责人▼   [搜索]    │
├─ 目录树 ──────────┬─ 资产列表 ───────────────────────────┤
│ 业务域             │ customer_order   表  负责人：李四     │
│ ├─ 订单            │ order_amount     字段  敏感：内部     │
│ └─ 用户            │ 月度收入         指标  已发布         │
└────────────────────┴──────────────────────────────────────┘
```

### 7.2 资产详情

```text
┌─ customer_order ─── 表 / 已发布 / 李四 ──────────────────┐
│ [查看上游] [查看下游] [提交治理任务]                        │
│ 概要 | Schema | 血缘 | 关联SQL | 质量 | 治理记录             │
├──────────────────────────────────────────────────────────┤
│ 字段名       类型       密级       关联指标                  │
│ order_amount decimal    内部       月度收入                  │
└──────────────────────────────────────────────────────────┘
```

默认打开“概要”，用户点击“血缘”才加载画布；画布超过 500 个节点时要求按方向、深度或资产类型缩小范围。

## 8. 智能指挥与作战室原型

```text
┌─ 订单跑批故障作战室 ────────────────────────────────────┐
│ 问题与影响：订单日报延迟       状态：处置中                │
│ [查看决策] [申请审批] [结束作战室]                         │
├─ 处置时间线 ───────────┬─ Agent 团队 ─┬─ 当前决策 ───────┤
│ 诊断完成                 │ 指挥官 完成   │ ○ 重试一次       │
│ 数据校验进行中           │ 日志分析 完成 │ ○ 等上游补数     │
│ 等待审批                 │ 数据验证 运行 │ ○ 转人工         │
└─────────────────────────┴───────────────┴─────────────────┘
```

用户先选择决策方案，再查看风险、收益、回滚和证据；禁止从 Agent 消息直接执行生产动作。

## 9. 任务与审批原型

任务详情固定显示：目标、来源、负责人、截止时间、当前状态、关联 Incident/问数/资产、证据和下一步。

审批详情固定显示：请求快照、风险等级、目标环境、参数差异、dry-run 结果、验证规则、回滚方案、审批意见。低风险批准按钮为“批准并执行”；高风险为“批准进入灰度”，Critical 只有“拒绝/转人工”。

## 10. 管理中心原型

数据源向导：`选择类型 → 配置网络 → 选择凭证引用 → 测试连接 → 选择采集范围 → 完成`。

模型向导：`选择 Provider → Endpoint → 凭证引用 → 能力探测 → 离线评测 → 设置数据边界 → 发布`。模型卡片显示本地/云、能力、上下文、延迟、评测分和当前路由。

## 11. 页面跳转图

```text
登录
 ↓
工作台 ──问一个数──→ 问数会话 ──转为任务──→ 任务详情 ──审批──→ 执行/验证
  │                     │                         ↑
  ├─查看事件──────────→ Incident 详情 ──转作战室─┘
  │                     │
  └─查数据资产────────→ 资产详情 ──提交治理任务──→ 任务详情
```

所有跳转携带来源对象 ID 和返回地址；完成子任务后返回来源页面并刷新状态。

## 12. 原型验收清单

- 登录、首次引导、工作台、问数、事件、资产、任务、审批均有页面入口。
- 每个页面有唯一主操作和明确完成态。
- 关键异常包括无权限、无数据、澄清、执行失败、审批超时、SSE 断线。
- 用户可从问数结果到任务、从事件到作战室、从资产到治理任务连续点击完成。
- 生产环境、风险等级、负责人、回滚和审计入口在详情页可见。
- 1280px 桌面宽度下三栏布局不重叠；1024px 下证据栏可折叠；移动端仅保留审批和事件跟进。
