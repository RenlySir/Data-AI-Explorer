# 智能问数模块开发说明

版本：v0.2

日期：2026-08-19

## 1. 当前实现

本轮已将智能问数从纯前端模拟扩展为可联调模块：

- TiDB MCP 元数据接口：Schema、表、字段类型、表 comment、字段 comment。
- TiDB MCP 查询接口：模型生成只读 SQL 后通过 MCP 执行，结果统一转换为表格和 ECharts Option。
- Text2SQL：配置 `MODEL_GATEWAY_BASE_URL` 和 `MODEL_GATEWAY_MODEL` 后调用 OpenAI-Compatible `/v1/chat/completions`；未配置时使用确定性只读 fallback，便于本地演示。
- 文件数据：上传 CSV/Parquet；扫描受允许根目录约束的本机目录；DuckDB 查询并返回结果和图表规格。
- 前端：原“TiDB 结构”升级为“数据关系”，可从注册数据源采集全量 Schema、表、字段 Comment 和外键，并以 ECharts 表级/字段级网络图展示；支持手工 SQL 与 TiDB 语句摘要增量学习关系，服务端持续采集任务在页面关闭后仍运行。
- ChatBI：新增独立数据源管理页、选择数据源对话分析、服务端图表类型推理、结果核验与认可、大屏报表复用。

### 1.1 用户操作路径

1. 从平台管理分组进入唯一的“数据源管理”入口，点击“添加数据源”，选择 TiDB/MySQL 并保存测试，或上传 CSV/Parquet。
2. 在可用数据源卡片点击“进入问数”；智能问数不再承载数据源管理，ChatBI 顶部数据源选择器会自动选中该数据源，也支持切换其他已配置数据源。
3. 输入自然语言问题并发送。左侧保留对话上下文，右侧展示结论、ECharts 图表、数据表、只读 SQL 和证据。
4. 用户核验后点击“认可并加入大屏”；大屏只展示被认可的报表，并保留原问题、数据源和认可人。
5. 点击“再次分析”回到同一数据源和问题；点击移除仅移除大屏卡片，不删除原数据源。

## 2. MCP 接入协议

后端作为 MCP Client，连接 Streamable HTTP endpoint。工具名先通过 `list_tools` 发现，再按别名匹配：

| 能力 | 默认工具别名 | 参数 |
|---|---|---|
| Schema | `list_schemas` / `get_schemas` | database |
| 表 | `list_tables` / `get_tables` | database、schema |
| 字段 | `describe_table` / `get_table_schema` | database、schema、table |
| 关系 | `list_relationships` / `list_foreign_keys` / `get_lineage` | database |
| SQL | `execute_query` / `query` / `run_sql` | sql |

如果企业 MCP Server 使用其他工具名，`POST /api/v1/tidb/mcp/introspect` 的 `tool_map` 可以显式指定：

```json
{
  "endpoint": "http://tidb-mcp.internal/mcp",
  "token": "短期令牌",
  "database": "analytics",
  "tool_map": {
    "schemas": "catalog_schemas",
    "tables": "catalog_tables",
    "columns": "describe_table",
    "relationships": "get_lineage",
    "query": "execute_readonly_sql"
  }
}
```

生产实现需要在 MCP Server 侧确保只读账号、库表白名单、查询超时、扫描量和审计；平台侧仍必须执行 SQL Guard，不能因为 MCP Server 存在就跳过安全校验。

## 3. API

```text
POST /api/v1/tidb/mcp/introspect
GET  /api/v1/tidb/catalog
POST /api/v1/query/conversations
GET  /api/v1/query/operations/{operation_id}
POST /api/v1/datasets/upload
POST /api/v1/datasets/local-directory
GET  /api/v1/datasets
POST /api/v1/datasets/analyze
GET  /api/v1/chatbi/datasources
POST /api/v1/chatbi/datasources
POST /api/v1/chatbi/datasources/{id}/test
POST /api/v1/chatbi/datasources/upload
DELETE /api/v1/chatbi/datasources/{id}
POST /api/v1/chatbi/query
GET  /api/v1/chatbi/reports
POST /api/v1/chatbi/reports
DELETE /api/v1/chatbi/reports/{id}
```

问数响应包含 `sql`、`columns`、`rows`、`chart.option`、`evidence`。前端不自行猜测指标含义，图表由服务端的 ChartSpec/ECharts Option 驱动。

## 4. 数据安全边界

- 只允许单条 SELECT/CTE；拒绝 DDL、DML、多语句和危险管理关键字。
- 本机目录读取必须落在 `DATASET_ALLOWED_ROOTS` 下，Compose 默认只读挂载 `/workspace/data`。
- 上传文件保存到服务端临时目录，生产需改为对象存储并绑定租户/用户权限。
- MCP token、模型 Key 不写入日志和数据库；当前 API 示例仍应通过 Vault/短期凭证接入生产。
- 文件分析使用 DuckDB，生产需增加最大文件大小、行数、执行时间、并发和内存配额。
- 数据库密码与返回模型分离，当前开发版仅在进程内存持有；生产必须替换为 Vault/OpenBao/KMS 的凭证引用。
- 直连数据库仅允许精确白名单，或在开发模式下解析为私网/回环地址；生产必须关闭 `CHATBI_ALLOW_PRIVATE_HOSTS` 的宽松模式。

## 5. TiDB 联调数据

`scripts/tidb_chatbi_demo.sql` 创建隔离库 `aegis_chatbi_demo`，包含客户、商品、订单、日期汇总等可重复生成的数据和 Comment。脚本仅清理自身专用表，不修改现有业务库。当前联调已验证一个可访问的 TiDB SQL 入口，读取到 5 个对象并跑通区域 GMV 聚合；具体服务器地址和凭证不进入仓库。

## 6. 进一步落地任务

1. 将 MCP 工具返回结构适配为正式 `CatalogSnapshot`，增加字段级 lineage 和外键/SQL 推导关系。
2. 将 `OPERATIONS`、`DATASETS` 和目录快照迁移至 PostgreSQL/对象存储。
3. 用 SQLGlot/Apache Calcite 替换当前轻量 SQL Guard，并接入 TiDB EXPLAIN 成本检查。
4. 增加真正的 Temporal 异步工作流、SSE 进度和取消/重试。
5. 将前端 Schema 关系列表升级为 React Flow/Cytoscape 画布，并支持按需展开。
6. 增加模型离线评测集、中文业务术语、指标语义层和结果复核规则。
